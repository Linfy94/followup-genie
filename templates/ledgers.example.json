{
  "_这是模板": "复制到 <运行时目录>/followup/config/ledgers.json 后按你的台账改。包内模板升级时可能被覆盖，运行时 config/ 下的不会。",
  "_铁律": "所有台账源只读。本文件只提供地址与字段名，不提供任何动作。",

  "ledgers": [
    {
      "id": "示例业务线",
      "_id说明": "内部标识，用于状态文件的键。定下来别改——改了等于所有催办状态重置。",
      "name": "台账文件的名字",
      "line": "业务线名字（出现在推送里）",
      "enabled": true,

      "source": "tencent_mcp",
      "_source说明": [
        "支持两种数据源，写错会直接报错、不会静默跑空：",
        "  tencent_mcp —— 腾讯文档。需要 file_id + sheet_id，凭证是 .env 的 TENCENT_DOCS_TOKEN。什么都不用装。",
        "  lark_cli    —— 飞书多维表格。需要 base_token + table_id + profile，",
        "                 靠外部的 lark-cli 命令行工具读（安装与四层授权见 docs/飞书多维表格接入.md）。",
        "                 完整字段见本文件末尾那个「示例飞书台账」。",
        "两种可以混用，一份配置里既有腾讯文档台账也有飞书台账是正常的；",
        "只用其中一种时，另一种的凭证不需要配，自检也不会要求你配。"
      ],
      "file_id": "从腾讯文档链接里取：docs.qq.com/sheet/【这一段】",
      "sheet_id": "子表 ID，链接里 ?tab= 后面那段；不确定就先随便填，报错信息会列出所有子表",
      "ruleset": "示例规则集",
      "_ruleset说明": "指向 rules.json 的 rulesets 里的某个键。必须对得上，否则拒跑。",

      "key_field": "序号",
      "_key_field说明": "项目的唯一标识列。用序号这类稳定值，不要用项目名称——名字会被改写。这一列是所有例外表和催办状态的主键，必须非空且唯一，否则会报错拒跑。",
      "name_field": "项目名称",

      "required_columns": ["序号", "项目名称"],
      "_required_columns说明": "入口断言。缺任何一列立即报错退出，不许继续跑出「今天没有超时单」。把规则用到的列都列进来。",

      "scope_filters": [],
      "_scope_filters说明": [
        "责任范围过滤。范围外的行压根不进催办流程。",
        "留空 = 全量处理。绝不默认收窄——静默漏催比多催更难发现。",
        "例：只关心某几个地区 → [{\"field\":\"地点\",\"op\":\"in\",\"values\":[\"甲地\",\"乙地\"]}]",
        "支持的 op：in / not_in / equals / not_equals / empty / not_empty / contains",
        "多个过滤器之间取 AND。每次运行都会报告被过滤掉多少行。"
      ],

      "known_values": {},
      "_known_values说明": [
        "取值白名单，用于发现写法不统一。",
        "例：{\"地点\": [\"甲地\",\"乙地\",\"丙地\"]} —— 出现「甲地市」这种写法时会警告。",
        "为什么重要：如果这一列同时用于 scope_filters，写法不统一的行会被静默丢掉，",
        "这是排查漏催时最难发现的坑。"
      ],

      "terminal_states": [],
      "_terminal_states说明": [
        "终止判定 · 优先级 1（权威，一票定案，不再看备注）。",
        "填台账里明确表示「这个项目结束了」的状态值。",
        "例：[{\"field\":\"技术确认\",\"op\":\"in\",\"values\":[\"不可行\"]}]"
      ],

      "terminal_column": {
        "enabled": false,
        "field": "终止",
        "truthy_values": ["终止", "是", "已终止"]
      },
      "_terminal_column说明": [
        "终止判定 · 优先级 1 的最佳形态：台账里有一个专门的「终止」列。",
        "强烈建议推动业务加这一列——它是业务让催办停下来的唯一干净方式。",
        "没有它的话，业务遇到「项目技术可行、客户也同意，但就是不推了」时",
        "没有正确的表达方式，只能看着它一直被催。"
      ],

      "terminal_note_phrases": [],
      "_terminal_note_phrases说明": [
        "终止判定 · 优先级 2：备注里的语义词。",
        "适用于业务在备注里写了「关闭」「不予实施」这类话但没改状态列的情况。",
        "⚠️ 这是过渡手段。词表对全表生效，一定会误杀，必须配 exceptions。",
        "台账加了「终止」列之后，这个降级为兜底补捞。"
      ],

      "terminal_note_exceptions": [],
      "_terminal_note_exceptions说明": [
        "语义词表的例外。格式：[{\"key\":\"主键值\",\"reason\":\"为什么不算终止\"}]",
        "两类必然出现的误杀模式（换业务线也会重复出现）：",
        "  ① 否定对象错位——「放弃【责任协议】」不等于放弃项目",
        "  ② 部分否定——一个项目多台设备，否定其中一台不代表整个项目停",
        "关键词永远分不清这两种，所以例外表是必需的，不是可选的。"
      ],

      "paused": [],
      "_paused说明": [
        "暂缓：等外部事件（客户在做别的改造、等厂家供货），短期催无意义。",
        "格式：[{\"key\":\"主键值\",\"reason\":\"原因\"}]",
        "⚠️ 必须与终止分开记。两者都不进日常催办，但暂缓的日后要回访，终止的不用。",
        "混成一类，日后想捞回暂缓项目就再也分不出来了。"
      ],

      "gray_list_for_review": [],
      "_gray_list_for_review说明": [
        "如果业务用底纹/颜色标记状态：把那批主键列在这里。",
        "⚠️ 定位是「待复核清单」，不是判据——底纹会标错。",
        "程序会把它和状态列/备注比对，把不一致的列出来供业务清账。",
        "底纹是格式不是数据：复制粘贴、另存、别人编辑都可能丢，任何下游工具都读不到。"
      ]
    },

    {
      "id": "示例飞书台账",
      "_这一条是干什么的": [
        "数据源在飞书多维表格时的完整字段示例。判定、终止、名单那些配置",
        "与上面那份**完全一样**（core 只认「节点→字段→阈值」这套抽象，",
        "不关心背后是腾讯文档还是飞书），所以这里只列出不一样的部分。",
        "不需要飞书就把整个这一条删掉。"
      ],
      "name": "飞书多维表格的名字",
      "line": "业务线名字",
      "enabled": false,

      "source": "lark_cli",
      "_前置条件": [
        "需要 Node.js 和飞书官方的 lark-cli：",
        "    npm install -g @larksuiteoapi/lark-cli",
        "安装后按 docs/飞书多维表格接入.md 配置命名 profile、Base scope、用户登录与文档协作者。",
        "每条 config/auth/base 命令必须显式使用与本条 profile 字段相同的名字。",
        "程序不会替你装，找不到时会停下来把命令打出来。",
        "只读白名单写死在 scripts/lark_base.py 的 ALLOWED_SUBCOMMANDS：",
        "只允许 +table-list / +field-list / +record-list 三个只读子命令。"
      ],

      "base_token": "多维表格的 base token。必须来自原始 /base/ 链接或 lark-cli 解析结果；/share/base/view/shr... 不能直接使用",
      "table_id": "数据表 id，形如 tblXXXXXXXXXXXX。一个 base 里每张表一个",
      "profile": "sentinel",
      "_profile说明": "lark-cli 的身份配置名，决定用谁的授权去读。缺了会报错，不会默默用错身份。",

      "key_field": "_record_id",
      "_key_field说明": [
        "飞书自己的记录 id，程序会把它当成一个普通列暴露出来，永远唯一。",
        "为什么默认用它而不是「企业名称」：同一家公司在不同分行各开一个项目很常见，",
        "人类可读的字段不保证唯一，而主键一旦重复，催办状态就会串台。",
        "想用业务字段当主键也可以，但那一列必须非空且唯一，否则会报错拒跑。"
      ],
      "name_field": "企业名称",

      "link_date_fields": [
        {
          "link_field": "主表上那个关联字段的列名",
          "child_table_id": "被关联的那张表的 table_id",
          "child_date_field": "去那张表里取哪一列日期"
        }
      ],
      "_link_date_fields说明": [
        "关联字段读出来只是一串记录 id，看不到日期。这里按配置把它换成",
        "关联记录里某个日期字段的值（多条取最早一条），这样规则里的 clock /",
        "empty / not_empty 可以直接用这个列名，不用另写跨表逻辑。",
        "不需要就写成 [] 或整个删掉。",
        "⚠️ 三个键缺一不可，写错会在自检时报错——以前是直接抛一屏英文堆栈。"
      ],

      "required_columns": ["企业名称"],
      "scope_filters": [],
      "_scope_filters提醒": [
        "🔴 同一个字段在不同表里写法可能不同。实测遇到过：一张表写「杭州」，",
        "另一张写「杭州分行」。照抄另一张表的取值会一条都匹配不上、",
        "把整表静默过滤光，而表现只是「今天没有要催的」。",
        "每张表的取值都要单独看一眼再写。"
      ],
      "known_values": {},
      "terminal_states": [],
      "manual_terminal": [],
      "terminal_note_exceptions": [],
      "paused": [],
      "gray_list_for_review": []
    }
  ]
}
