# 变更记录

## 0.6.0-rc8 · 2026-08-14

**让 2026-08-14 那类故障一分钟能定位，而不是一小时。**

那天连撞三个故障（企微机器人文档权限到期、代理搞坏 TLS 1.3、
飞书日期列整列解析失败），三个都不报错、三个都长得像「今天没有要催的」。

- **自检报出运行环境**：解释器路径、Python 版本、SSL 后端。
  这台机器上有三个 Python、三套 SSL 后端，排查那天有一半时间耗在
  「我自己用错了解释器」和「真故障」分不开上。
- **解释器与定时任务不一致会自动告警**。真实运行把指纹记进健康记录，
  自检拿自己的跟它比。**不写死路径** —— 写死只在这一台上成立，
  同一个包也装到 WorkBuddy 和业务电脑。
- **自检报出本次是否降级到 TLS 1.2**，并说清「程序扛住了、但网络该查」。
- **「整列日期全废」独立成一条醒目告警**：说明「依赖它的节点一条都不会催」。
  此前它和「几行没填」长得一模一样 —— 飞书那次整列 731 行失效、
  3 个项目无人管，就是这么被盖住的。
- 企业微信错误对照表补 `851014`（**实测 2026-08-14**）：机器人「文档」
  使用权限到期，须由创建者在企业微信续授权，**重新扫码无效**，实测约 9 天一轮。

## 0.6.0-rc7 · 2026-08-14

**补上 TLS 兜底的另一半：两个外部命令行工具也会降到 TLS 1.2。**

- rc3~rc4 只修了 Python 侧的两个出口。`lark-cli` 与 `wecom-cli` 都是 Node、
  **有各自的 TLS 栈**，管不到 —— 当天 Node 侧仍打挂一次生产运行
  （`lark-cli：remote error: tls: bad record MAC`，退出码 1、少读一份台账）。
  **修完一半比没修更危险**：Python 侧全绿会让人以为整件事结束了。
- 撞上 TLS 失败时给子进程加 `NODE_OPTIONS=--tls-max-v1.2` 重试一次。
  判据与降级动作住在 `cli_env.py`，两个 CLI 共用同一份。
- 与 Python 侧同样**不写死 1.2**：本进程内粘滞，重启重新试 1.3。
- 授权过期、文档无权限、超时这些明确故障不会被当成 TLS 问题重试掉。

## 0.6.0-rc6 · 2026-08-14

**数据质量告警只报真正要动手的行；哨兵④发货改用方案完成日计时。**

业务 2026-08-14 逐条核对后定的口径。

- 告警收窄到「责任范围内 + 未终止 + 该节点判据真的命中」。此前扫全表，
  每天报的几乎全是噪声：舆情「日期」报 1056 行为空（全在责任范围外）、
  47 行解析失败（在范围内的 5 行全是「已交付」）；GEO 报 256 行空值
  （范围内 0 行）。**告警太吵和告警缺失是同一种失效** —— 飞书「立项时间」
  整列解析失败、3 个项目无人管，就是被这堆噪声盖住的。
  实测：每天 7 条降到 3 条，且剩下的全是「范围外」一行式预警。
- 🔴 `known_values` 里用于 `scope_filters` 的列**仍然扫全表** ——
  那正是「台账把『深圳分行』改成『深圳』导致整表落空」的护栏，
  先按范围过滤再检查等于用出问题的条件去筛出问题的证据。
- 哨兵④发货：判据加「方案完成情况 contains 已完成」，计时起点从「立项时间」
  改为「方案完成情况」里的日期。此前只判「发货表为空」，把连方案都没评估的
  项目也算进来了（13 行里只有 5 行真的在等发货）。
- 适配层支持从「已完成,2026年08月07日」这类文本里抽日期，
  **只认完整年月日** —— 只写 '2026' 的行不猜，猜出来的起点比没有更糟。

## 0.6.0-rc5 · 2026-08-14

**修复：飞书日期列认不出 ISO 格式，3 个项目此前既不催也不终止。**

- 哨兵飞书主台账「立项时间」实际返回 `2025-10-24T16:06:34.000+08:00`
  （带 T、毫秒、时区偏移），而适配层只认三种 `strptime` 格式，
  **731 行全部解析失败**。配置注释里还写着「72/72 都能解析」，
  说明输出形状在某次之后变了，而这件事不报错。
- 后果：④发货 拿不到计时起点，深圳宝田医院、长城国际信息技术（深圳）
  有限公司、银图电器（深圳）有限公司三个项目落进「没有任何节点管」。
- 修复后 731/731 可解析；名单比对只出现 6 处差异，全部可解释：
  三行从 `no_node` 进入正常跟踪，三条「无可用计时起点」警告消失。
  **没有任何项目因此不再被催。**

## 0.6.0-rc4 · 2026-08-14

**修复 rc3 的降级只覆盖了三分之一的失败形态（复查发现，P0）。**

- rc3 只捕裸 `ssl.SSLError`，且把响应体读取留在调用方。实测
  `urllib` 只包装 `h.request(...)` 那一段，同一个 bad record mac 有三种长相：
  连接/握手/发请求 → `URLError(reason=SSLError)`（**rc3 漏**）、
  读响应头 → 裸 `SSLError`（rc3 命中，当天恰好是它，所以真机验证全绿）、
  读响应体 → 裸 `SSLError` 但发生在模块外（**rc3 漏**）。
- 三种现在都覆盖：响应体读取收进模块内，并识别 `URLError` 里包着的 SSL 错误。
- `HTTPError` 与非 SSL 的 `URLError`（DNS、连接被拒、超时）明确不重试 ——
  否则凭证失效会被当成网络抖动，调用方的重试次数也会悄悄翻倍。
- 区分请求阶段与响应阶段失败：请求阶段重试绝对安全；响应阶段说明请求已
  完整送达，对企微推送这类「发出去收不回」的调用会**额外提示可能重复**。
  仍然重试 —— 宁可重复，也不静默漏催。

## 0.6.0-rc3 · 2026-08-14

**修复：本机代理搞坏 TLS 1.3 时，催办清单推不出去。**

- 2026-08-14 实测：业务电脑常开的代理会把所有 TLS 1.3 记录搞坏
  （腾讯文档、企业微信文档、企业微信推送、飞书、乃至 Google 全部连不上，
  TLS 1.2 全部正常）。当天读数正常但**推送 0/1 条失败，业务没收到清单**。
- 腾讯文档取数与企业微信推送改走同一个新模块 `scripts/nethttp.py`：
  TLS 握手失败时自动降到 TLS 1.2 重试一次，**并在日志里说明降级原因**。
- 降级在单次运行内粘滞，不让每个请求都白赔一次握手；
  **进程重启会重新尝试 TLS 1.3**，网络修好当天自动恢复，不需要任何人动手。
- 降级只封顶协议版本，证书校验与主机名校验一律不动。
- 降级若也失败，抛出的是**最初那个** TLS 错误，不掩盖真实原因。

## 0.6.0-rc2 · 2026-08-12

**安全修复：企业微信文档取数失败时不再泄露访问凭证。**

- 企业微信命令行的原始错误、JSON 外层错误、接口错误信息与轮询超时 URL，
  均在进入日志、故障告警和 `health.json` 前统一脱敏。
- 新增回归测试，覆盖查询参数、`access_token`、Bearer 凭证和轮询 URL；
  测试凭证不允许出现在任何异常文本中。

## 0.6.0-rc1 · 2026-08-12

**主题：需求变了，规则要跟得上。** 业务反馈「需求和提醒事项会随流程优化改变，
希望催办节点可配置、变更能快速响应」。催办节点本来就是配置（改 `rules.json`
不改代码、不发版、不部署，次日 9:00 生效），所以这一轮做的是**让改动更快、更不容易改错**。

### 新增

- **需求文档变更探测**（业务能感知）。每天推送前自动核一次《跟进精灵需求文档》
  有没有被改过，变了就在当天的清单里出现一条 `📌` 提示，说清是谁在什么时候改的。
  **只回答变没变，不回答变了什么** —— 价值在于文档没变的那些天，那次人工核对
  完全可以不做。取指纹用的是本来就在只读白名单里的 `manage.query_file_info`，
  零新增能力、零 token。
  - 配置：`rules.json` 顶层的 `spec_watch`（`file_id` + `name`）
  - 🔴 提示**持续到人工确认为止**，不是只响一天。核对完跑
    `check_followup.py --ack-spec` 消除
  - 提示不影响退出码，也不影响「今天算不算跑成功」——
    需求文档跟催办判定无关，读不到它不该让当天的催办算失败
- **`doctor.py --values <台账id>`**：只读列出这份台账里判据用到的每一列的
  **真实取值**。改触发条件前必看 —— 照抄业务口头说的写法会一条都匹配不上
  （实测：分行有「杭州」/「杭州分行」两种写法；GEO 的「启动优化时间」全表 43 种）。
- **`scripts/diff_due.py`**：比对两次 `--dry-run --json` 的结果，
  逐条列出新增催办 / 不再催办 / 超期天数变化 / 去向计数变化 / 警告增减。
  把「改动前后名单逐条比对」从肉眼活变成一条命令。
- **`docs/改一个催办节点.md`**：需求变了怎么改规则的操作卡，按改参数 /
  改触发条件 / 增删节点三类分节，每类给完整命令。

### 修复（都是**不报错**的那一类）

以下写法此前能过 JSON 语法、能过离线校验、运行时也不抛错，
只是让那个节点从此不再产生催办 —— 一条日志都没有。现在离线校验一律拦下：

| 写法 | 原后果 |
|---|---|
| `{"op": "in"}` 漏写 `values` | `取值 in []` 恒假，该节点一条都不催 |
| `{"op": "contains"}` 漏写 `value` | `"" in 取值` 恒真，该节点吞掉所有项目，后面的节点全部轮空 |
| 条件漏写 `field` | 取值恒为空串，判据静默失效 |
| 启用节点的 `when: []` | 恒假，节点永远不命中 |
| `enabled: "false"`（字符串） | 字符串是真值，想停用却把节点静默开着 |
| `op` 写错（`contain` / `eq`） | 要等到次日 9:00 运行时才抛错 |

🔴 `scope_filters` 走同一套条件匹配，同样的写法在那边后果更大：
**整张台账被过滤光**，表现只是「今天没有要催的」。两处共用同一份校验。

### 内部

- 条件 op 清单提成 `core.CONDITION_OPS`（按要带 `value` / `values` / 不带取值
  分三组），离线校验与运行时共用同一份，不各写一遍。
- 测试 643 → 742。

## 0.5.0-rc3 · 2026-08-10

**收口三来源接入：所有业务入口按数据源分流，修正企微 `851002` 的无效建议。**

- README 唯一业务指令、WorkBuddy 代理指令和自动化提示词全部改为“所有台账源只读”，
  不再只约束腾讯文档。
- WorkBuddy 首次接入先识别 `source`：只有腾讯文档 Token 与消息 Webhook 写 `.env`；
  `wecom-cli`、`lark-cli` 身份保留在各自凭证存储中，不再无条件索取腾讯文档 Token。
- `851002` 按失败命令分流：结构探针失败时核对 URL / 文档类型；正文读取失败时明确
  当前适配器不兼容，不再让业务修改请求中根本不存在的 `sheet_id`。
- 补齐企微文档网络地址、纯企微用户无需腾讯凭证等说明，并扩展回归测试覆盖所有业务入口。

## 0.5.0-rc2 · 2026-08-10

**补齐腾讯文档、企业微信文档的从零接入引导与错误分流。**

- 新增 `docs/腾讯文档接入.md`：明确 Token 是授权凭证而非本机登录态；必须由
  能打开目标台账的同一账号在 OpenClaw 专用入口扫码，并把“缺 Token / 失效 /
  文档无权 / 链接字段错误”拆成四种处理动作。
- 新增 `docs/企业微信文档接入.md`：明确读表走 `wecom-cli` 的**智能机器人**，
  与推送用的群机器人 Webhook 是两种不同身份；补齐 Node.js、安装、初始化、能力
  授权、文档对象权限和只读探针步骤。
- 企业微信错误码新增明确分流：`851008` 是机器人能力缺失，`851003` 是机器人对
  目标文档没有对象权限，`851002` 是链接或文档类型不兼容。不会再笼统建议重新扫码。
- 所有入口、模板和业务说明统一列出三种数据源；新增接入文档回归测试。

## 0.5.0-rc1 · 2026-08-10

**只换版本号，功能与 `0.4.0-rc12` 完全一致。**

小版本从 0.4 跳到 0.5，是因为 **rc10 接入企业微信文档时新增了一个数据源**
（`wecom_doc`），当时按补丁级往下排了。这与 0.4 那次跳级是同一个理由：
0.3 → 0.4 是因为飞书线引入了外部依赖 `lark-cli`，**依赖声明变了**；
这次是 `wecom-cli`，同样不是补丁级改动。

照旧版说明装机的人会漏装东西 —— 这是跳 minor 的实际后果，不是编号美学。
现在三种数据源的依赖是：

| 数据源 | 需要装什么 |
|---|---|
| 腾讯文档 | 什么都不用装（只要 `TENCENT_DOCS_TOKEN`）|
| 飞书多维表格 | Node.js + `lark-cli`，四道门授权见 `docs/飞书多维表格接入.md` |
| **企业微信文档** | **Node.js + `wecom-cli`**，外加机器人的「获取成员文档内容」能力授权 |

**仍然带 `rc` 后缀**：WorkBuddy 业务电脑未实机验收、Windows 从未验证、
企微那项能力授权会不会过期至今未知。这三条没结论之前不该叫正式版。

## 0.4.0-rc12 · 2026-08-10

**修复飞书从零安装引导：登录和协作者不是二选一，必须按四层顺序完成。**

- 新增独立接入手册 `docs/飞书多维表格接入.md`，顺序固定为：命名 profile →
  Base API scope → 同一 profile 的 user 登录 → 该登录账号的文档协作者权限。
- 所有登录示例显式带 `--profile`，避免业务登录默认 profile、程序却读取
  `sentinel` profile。
- 授权只申请 `bitable:app:readonly` 与定时刷新所需的 `offline_access`；不申请
  多维表格写权限。
- 分享视图 `/share/base/view/shr...` 不再被描述为可直接取得 `base_token`。
- 错误处理保留 lark-cli 的结构化分类；缺 scope、未登录、91403 资源权限、
  profile 未初始化和钥匙串不可用分别给不同建议。
- `doctor` 删除「多半重新 auth login」的统一猜测，避免让业务反复做无效操作。

## 0.4.0-rc11 · 2026-08-10

**只加了一组测试，代码零改动。** 业务提了两条关于节假日的要求：
「每天提醒」要跳过节假日、「每 7 天提醒」遇节假日则在假后触发。
实测**两条都已经是现在的行为**，所以这一版做的是把它们钉住 ——
已有行为最容易在后续改动里被无声改掉，而症状是「假期后该催的没催」。

`tests/test_cadence_across_holidays.py`：假期当天不推、不消耗节律；
到期日落在假期里时，假期后第一个工作日**立刻补上**而不是再等一轮。
另把它成立的两根柱子单独钉住（非工作日不投递、没投递就不写「已通知」）——
拆掉任一根，这组用例都会红。

## 0.4.0-rc10 · 2026-08-10

**接入企业微信文档主台账，舆情 / AI体检 / GEO 三条线终于有了「交付」侧的节点。**
卡了五天的机器人能力授权当天通过，从探结构到上线一次做完。

业务能感知的：

- **舆情、AI体检报告、GEO 三条线各多了后续节点**，且与原有的前期节点
  **合并成同一节**、编号从 ① 接到 ②③（靠 `display_name` 复用现成逻辑，
  渲染层一行代码没加）。
- 新增 5 个节点：舆情②交付、AI体检②确认词包、AI体检③制作和交付、
  GEO②确认词包、GEO③交付。
- 接入当天：舆情催 0、AI体检催 0（都是「有项目但还没到阈值」，
  不是没接上）、GEO 催 7。六份旧台账的待催名单**逐条不变**。

引擎侧：

- **新数据源 `wecom_doc`**。只读白名单只有 `sheet_get_info` /
  `get_doc_content` 两个命令；`wecom-cli doc` 底下实测 25 个子命令，
  其中 8 个能改能删，永远不进白名单。
- **`key_field` 支持组合键**（写成数组）。三份企微台账一个唯一列都没有，
  不支持组合键就一行都读不了。
- **计时起点支持人工播种 `manual_stage_entered`**，给一个可靠日期列都
  没有的台账用。播种排在真实快照**之后**，快照永远优先。
- **主键断言改为范围感知**：责任范围外的空主键/重复主键降级成数据质量警告，
  不再为几行范围外的脏数据停掉整条业务线。系统性读失败仍然致命。
- `cli_env.py`：找 CLI、拼子进程 PATH、剔 Agent 上下文变量三件事
  由 lark-cli 与 wecom-cli 共用一份实现。

🔴 **`851008` / 授权失效不会表现成「今天没有要催的」**：实测 JSON-RPC 外层
`"isError": false`，错误藏在第三层信封的 `errcode` 里，代码逐层检查。
授权会不会过期至今没有答案，这道检查就是那个未知数的兜底。

## 0.4.0-rc9 · 2026-08-10

**节律写错了必须当场报错 —— rc8 把改规则的权交给业务，这一版补上护栏。**
（问题全部由 Codex 复审提出，复现时发现还比报告的更严重。）

业务能感知的：

- **`doctor` 的口径表显示的是人话**：原来配了数组会打印成
  `只在每周['Mon', 'Thu']提醒`，现在是「周一/周四提醒」，与推送里的措辞一致。
- **配了 `monthday` 的节点不再被 `doctor` 误报「缺 repeat」**。
- **节律写错时 `doctor --validate-config` 当场报错**，不用等第二天早上定时任务出问题。

引擎侧（`repeat` 校验收紧，四条都是原来放行的）：

- `repeat` 不是对象（如 `"repeat": "每周一"`）→ 原来运行时 `AttributeError`。
- `days` / `workdays` 必须是 **≥1 的整数**。原来 `-3` 会**静默变成每天提醒**
  （`(今天−上次).days >= -3` 恒真），`"七"` 运行时炸，`7.9` 被静默截断成 7。
- 排他检查改成看**键在不在**，不看值真不真：`{"weekday": [], "days": 7}`
  原来被当成「只配了 days」放行 —— 那正是改到一半的形状。
- `doctor` 里那份自带的 `repeat` 解读**删掉**，与主流程共用 `core` 的同一份。
  它漏掉 rc8 新增的 `monthday` 就是复制实现必然的走样。

判定逻辑一行未动，待催名单与 rc8 逐条一致。

## 0.4.0-rc8 · 2026-08-10

**按业务的需求文档校准节律，并让「业务改提醒规则」不再需要改代码。**

业务能感知的：

- **哨兵④发货改成「每周一和每周四」提醒**（原为每周三）。需求文档早已改过，
  配置没跟上——于是周一、周四她看不到待发货的项目，而她正是在周一来问的。
- **推送的阶段行会显示当前节律**，例如「【待发货】3 项 · 周一/周四提醒」。
  规则改了当天就能核对生效的是什么。
- **盒子①收资停用**：需求文档里已无此节点。报告里会打出
  「⏸ ①收资：未启用，不会产生催办」——停用是明说的，不是悄悄消失。

引擎侧：

- `repeat` 支持四种写法，且**只能配一种**：`days` / `workdays` /
  `weekday`（可给数组）/ `monthday`（新增，每月固定几号；配 31 时
  当月不足则按月末算，不静默跳过整月）。
- 同时配两种以前会**静默忽略一种**（`if weekday … elif workdays … else days`），
  现在在配置校验期直接报错——业务改节律最常见的动作就是
  「加一行新的、忘了删旧的」。
- 日志新增「【静默期】N 项」清单，含上次提醒日与节律。
  **不进企微推送**：给业务的推送只回答「今天该做什么」。
  在此之前，「已催过在等下次」与「判定认为不用催」在任何地方都分不出来。
- repeat 的检查同时进了**离线校验**，`doctor --validate-config` 现在拦得住。
  在此之前它只住在 assert_sheet 里、要真读到台账才跑，于是对着一个会炸的
  配置报「全部通过」——与 rc6 的 `alert.timeout_seconds` 同一个形状：
  **离线一道、运行时一道，两道都要有。**

⚠️ **一处退出码变化**：`repeat` 配错/缺失从退出码 `1` 变成 `2`。
因为它现在在碰台账之前就被拦下，而按 README 的退出码表，「配置错」本就归 `2`、
「入口断言不过」才归 `1`。两者对宿主都是「报失败」，告警照发，watchdog 不受影响
（它看的是 `health.json` 的 `last_full_success`，不是这个退出码）。

## 0.4.0-rc7 · 2026-08-06

**补 rc6 自己带进来的一个坑：范围过滤告警发送失败，却被记成「已告警」。**

- 上一版只要判定出「整表落空」就写进去重登记，不看告警有没有真的发出去。
  告警失败那次恰恰是最需要它的时候（网络抖动、hermes 不在 PATH），
  而登记一旦写下，后续每天都被去重跳过——这条业务线从此永久静默。
- 现在只有告警**发送成功**才登记；失败不写登记，下一次真实运行继续重试。

## 0.4.0-rc6 · 2026-08-06

**代码审查提的两条 P1，验证成立后修掉。**

- **告警配置写错不再让程序崩掉。** `alert.timeout_seconds` 填「三十」这类
  非数字时，告警函数会在重试之前抛异常——而它只在已经出事时被调用，
  等于把「有故障」升级成「连故障是什么都说不出来」。现在离线校验会拦，
  运行时也会退回默认 30 秒继续告警。
- **业务线被责任范围整表过滤掉时会主动告警一次。** 此前只在本地报告里写一行；
  那一刻待催数是 0，企微不发、告警不响、存活监控只看到「任务成功」，
  没人翻日志就等于全量失效而无人知晓。现在变空那天告警一次，
  一直空着不重复，恢复时记录一笔。退出码仍是 0——一条线暂时没有范围内
  项目是合法状态，不该报故障。

## 0.4.0-rc5 · 2026-08-06

**故障告警不再被一次网络抖动整条打掉。**

rc4 把告警失败的原因记全之后，当天就查清了 09:00 那次的真因：不是环境、
不是配置，是 Telegram 一段约 50 秒的网络抖动，而 `hermes send` 一次都不重试。

- 告警改为三次尝试，间隔 2s / 5s；每次失败的原因都记进 `health.json`。
- 重试只影响「发不发得出去」，不影响判定：三次都失败仍然记为未通知，
  绝不因为重试过就当作已送达。

依据：告警成功的 08-03/04/05 三天，同一时间窗内 Telegram 网络错误数都是 0；
失败的 08-06 是 4 条。同一天每日新闻的推送撞上同样的错误，
日志写着 `retrying in 1s (attempt 1/3)`，重试后送达。

## 0.4.0-rc4 · 2026-08-06

**修复 rc2 那个 P0 只修了一半留下的复发，外加两条让故障不再伪装成「今天没有要催的」的护栏。**

### 业务能感知的变化

- 飞书两条线在定时任务下恢复读取。rc2 之后它们仍连续两天整天无数据，
  表现与「今天没有要催的」完全一致。
- 某条业务线的台账被责任范围整表过滤光时，报告里会明确说出来，
  并列出实际观测到的取值 —— 此前只显示「总任务量：0 个项目里」。

### 修复

- `lark-cli` 的 Agent 上下文探测是一张固定清单，rc2 只剔除了其中两个变量。
  实测补齐为六个（新增 `HERMES_EXEC_ASK`、`HERMES_GATEWAY_TOKEN`、
  `HERMES_SESSION_KEY`、`HERMES_QUIET`）。真凶是 `HERMES_EXEC_ASK`：
  Hermes gateway 在进程启动后注入，定时任务原样继承。
  **刻意不改成前缀通配** —— 实测编造的同前缀变量并不触发探测。
- 故障告警失败时把子进程的 stdout 一并记入原因。此前只看 stderr，
  遇到「退出码非零但 stderr 为空」只会留下一句退出码，事后无从查起。
- 责任范围过滤后无剩余行时记数据质量警告。此前 `known_values` 只校验状态列，
  过滤字段换了写法（例如「深圳分行」改成「深圳」）会整表落空且完全静默。
  该警告不影响判定与退出码 —— 一条线暂时没有范围内项目是合法状态。

## 0.4.0-rc3 · 2026-08-05

本轮将生产树 `8eef7d8` 的行为与安装修复正式收口到唯一 GitHub 源码树。

### 业务能感知的变化

- 腾讯文档前期台账与飞书主台账合并成一个「AI 哨兵」区块。
- 「总任务量」只计责任范围内的项目；合并后再扣掉已从前期台账接力到飞书的重复行。
  因此飞书线不再显示「705 个项目里」，而是按责任范围显示。
- 节点日期支持按顺序多级兜底；主字段空但次级字段可用时不再误报「会被跳过」。

### 准确性与交付

- 跨台账接力支持可配置的多字段联合身份，并应用目标台账自身的责任范围。
  目标侧多条同身份时继续催办并列入待复核，不再冒险静默停催。
- `clock.fallback` 的数字、对象和混合数组会在离线配置检查时以人话拒绝，不抛原始堆栈。
- 发布器新增虚构组织白名单和中文组织名扫描，未登记的「公司/集团/医院/酒店/合作社」名称会直接阻止打包。
- 新增脱敏的 AI 节能盒子 + AI 哨兵可迁移预设；飞书仍需 Node.js + `lark-cli`，程序不自动安装。
- 首次安装带已核对的 2026 年节假日预设（33 个放假日、6 个补班日）；重复安装仍绝不覆盖已有 `holidays.json`。
- 自动化用例数以每次实际测试输出为准，不再把会过期的固定数量写进文档。

## 0.4.0-rc2 · 2026-08-05

**修复定时任务下飞书台账无法读取的真实故障。**

Hermes gateway 注入的 `HERMES_HOME` 被原样传给 `lark-cli`，导致后者误判为
Agent 上下文并要求先 `config bind`，未绑定就拒绝执行。该环境变量只出现在
gateway 派生的定时任务中，因此终端手工测试无法复现。

新的 `_child_env()` 会剔除 `HERMES_HOME` / `OPENCLAW_HOME`，再设定程序需要的
notifier 开关和 PATH。调用仍显式携带 `--profile`，不依赖 context 绑定。
新增真走 `_run_cli` 的子进程环境测试与 AST 源码守卫，防止后续外部 CLI 重复踩坑。

## 0.4.0-rc1 · 2026-08-05

小版本跳到 0.4：**依赖声明变了**。此前 README 与 SKILL.md 写的是
「第三方依赖：无」，而飞书那条线需要外部的 `lark-cli`。这不是补丁级改动，
照旧版说明装机的人会漏装东西，所以不走 rc5。

### 🔴 业务能感知：调休补班日现在会推了

2026 年有 6 个补班日（1/4、2/14、2/28、5/9、9/20、10/10），**全落在周六周日**。
上一版把定时表达式的星期字段写死成周一至周五，那几天业务在上班，
定时任务却根本不触发——脚本连判断的机会都没有，而它看起来和
「今天没有要催的」一模一样。

现在定时任务**每天** 9:00 只负责叫醒一次，发不发由脚本按
`config/holidays.json` 判断，三种情形收敛到 `WorkdayCalc.is_workday()` 一处：

| | 推不推 |
|---|---|
| 普通周末 | 不推 |
| 法定节假日 | 不推 |
| **调休补班日** | **推** |

🔴 **随之而来的新代价**：节假日表漏拷、或 `workday.exclude_holidays` 是 false 时，
法定假日会**照发**、补班日会**不发**。以前定时表达式还兜着周末，现在没有了。
`doctor` 会就此点名报警。

> 升级已装好的 Hermes：`hermes cron edit <job> --schedule "0 9 * * *"`。
> 只换包不改定时的话，补班日仍然收不到。

### 🔴 业务能感知：同一家企业的多个项目不再被合并成一条提醒

提醒事项的标题是「业务线＋阶段＋企业名」，而同一家企业在同一条业务线上
完全可以有两个项目（台账里不同序号的两行）——标题一模一样。
旧版按标题精确匹配认回旧提醒，于是**两条催办被合并成一条**：
业务只看到一个、漏掉另一个，而列表看起来还很干净。实测旧代码
两个不同项目只建出 1 条提醒。

身份改用 `台账|序号|节点`（与催办状态同一把钥匙），对应关系存进
`state/reminder_map.json`。**业务看到的标题一个字没改**，不塞技术编号也不塞天数。

升级当天会先按标题认领一次现存的旧提醒，所以**不会整批重建**；
历史遗留的「两个项目共用一条」会在那一次自动拆开。

### 飞书多维表格：从「能跑」到「装得明白」

- README 新增「按数据源看依赖」：接腾讯文档什么都不用装；接飞书需要
  Node.js + `lark-cli`。**程序绝不自动安装**，找不到时停下来说清怎么装。
- **纯飞书用户不再被要求配腾讯文档凭证**。`doctor` 以前无条件查
  `TENCENT_DOCS_TOKEN`，会给这类用户报一条红色的「凭证缺失」——
  自检报红是最强的「别用」信号，而正确做法本来就是不配。
- 模板删掉过时的「lark_cli / wecom_cli 是占位，未实现」，补一份完整的飞书台账示例。

### 配置写错不再静默或裸崩

| 写错什么 | 原来 | 现在 |
|---|---|---|
| lark_cli 台账缺 `base_token`/`table_id`/`profile` | 取数时 KeyError，一屏英文堆栈 | 离线自检就报，指名是哪一份台账 |
| `link_date_fields` 结构错 | KeyError / TypeError | 说清是第几项、缺哪个键 |
| **两份台账用同一个 id** | **完全不报错**，共用状态文件互相静音 | 直接拒跑 |
| `cross_ledger` 指向不存在的台账 | 跑到那个节点才炸 | 离线自检就报 |
| `cross_ledger` 的**字段名**写错 | 取值集合是空的 →「一条都没进主台账」→ **那个节点永远催下去** | 读到表时立即报错 |

### 升级失败会自动退回原版

原来的顺序是「先覆盖，再让 setup 去发现问题」。包缺文件、脚本有语法错、
自检不过——全都要等覆盖完才暴露，而那时旧版已经没了，定时任务第二天
照样会在半升级的代码上跑。

现在：**先校验新版**（清单齐全 + 关键入口在 + 每个 `.py` 能编译 + VERSION 非空，
只编译不 import）→ 存一份旧版 → 复制 → 跑自检；任一步失败就原样放回去。
新版多出来的文件也会被移出执行路径，否则仍是半升级。
现场全部移进 `.upgrade-rollback-*/replaced/`，**不删除任何东西**；
业务配置、状态、凭证全程不在清单范围内，逐字节不动。

### 其它

- 非工作日不推时，措辞区分「周末」与「法定节假日」——以前一律说成后者，
  业务在普通周六看到会以为节假日表配错了。

---

## 0.3.0-rc4 · 2026-08-03

### 🔴 业务能感知：法定节假日不再推送

定时表达式 `0 9 * * 1-5` **只排得掉周末，排不掉国庆春节**。而
`check_followup.py` 里根本没有「今天是不是工作日」这个判断 ——
节假日表只喂给②的复提醒计算，从不参与「今天推不推」。

于是国庆 10/1–10/7 里有五天照推、春节九天连休里有五天照推。
业务在放假，每天早上 9 点收催办。

现在脚本补第二道闸门（`notify.skip_non_workdays`，默认开）。

**假期里没推的项目不会被记为「已通知」**，节后第一个工作日会完整推一遍 ——
复用了已有的「无需投递」语义，不构成送达凭证。

🔴 **只拦投递，判定与健康记录照常。** 这一点看着多余，实则是必需的：
外部存活监控的 `missed_runs()` 只跳周末、**不认识节假日**（watchdog 刻意不
import core，拿不到节假日表）。假期里若静默退出、不写 `last_full_success`，
它会把国庆七天里的五个工作日班次数成「错过 5 次」，而告警阈值是 2 ——
**国庆第二天就误报「任务根本没跑」**。修好了假期骚扰，换来假期误报故障。
`tests/test_holiday_push.py::HealthMustStillBeWrittenTest` 专门守这条。

⚠️ **业务电脑上要等节假日表拷过去才真正生效**：`is_workday()` 只有在
`exclude_holidays` 为 true 且表已加载时才认识节假日，否则它只排周末 ——
而定时本来就排了周末，等于维持现状。漏拷由下面那条 `doctor` 警告负责喊。

⚠️ **补班日仍收不到**（2026 有 6 个）：那几天是周六周日，定时 `1-5` 根本不触发，
脚本没机会判断。已记入 `KNOWN_ISSUES.md`，是选定方案的已知代价。

### 装机漏拷节假日表原本完全无声

配置不随包分发（升级不覆盖是同一个设计的两面），所以业务电脑装完拿到的
`holidays.json` 是空模板、`workday.exclude_holidays` 默认 `false`。
而 `WorkdayCalc` 在 `exclude_holidays` 为 false 时**直接返回，一句话都不说**——
`doctor` 那一项甚至报的是 ✅ OK。

于是「每 2 个工作日复提醒」实际按「只排周末」在算，国庆七天连休会被整段
当成工作日，而没有任何东西会提。

现在：只要**真有节点**用 `{"workdays": N}` 复提醒而节假日表没启用，
`doctor` 就报 ⚠️ 并点名是哪个节点、给出两条修法。全部节点都是自然日时
仍然显示「无需节假日表」——关掉节假日本身是合法取舍，只有在有人依赖它时才是问题。

新增 `core.nodes_using_workdays()`，`doctor` 与每日运行共用同一份判断
（分头各写一遍迟早漂移，而漂移的表现是「doctor 说没问题，实际算错」）。

README 补上「装机时它是空的」以及需要做哪两件事。

### 测试

**325 → 344**。新增 `tests/test_holiday_push.py`（13 条）。

每条都在改动前的代码上验过会失败：节假日那批报「国庆假期里不该往业务群推催办」、
「节假日没投递，不该有投递凭证」；工作日口径那批报
`module 'core' has no attribute 'nodes_using_workdays'`。

## 0.3.0-rc3 · 2026-08-03

**上一版的修复自己带进来一批新问题，这一版收拾干净。**

`0.3.0-rc2` 为了堵住「创建中的空锁被当成损坏锁夺走」，让读侧遇到空锁一律让路。
**这个修法制造了一个新的永久死锁**：进程若在建锁与写内容之间被强杀（关机、OOM、
磁盘满），盘上会永久留下一个 0 字节的锁文件，从此没有任何进程能拿到锁——
表现是每天静默跳过，而这和「今天没有要催的项目」长得一模一样。
**与它本要修的第一个问题是同一类，出在同一个函数里。**

判定逻辑与业务规则仍然一行没动。

### 修的问题

| 问题 | 原来会怎样 |
|---|---|
| **空锁文件让运行永久静默** | 见上。实测连试五次，五次全部 `LockBusy` |
| **并发抢锁、释放锁、临时文件互相干扰** | 多个任务同时启动时会互相覆盖对方的临时文件，把一次正常运行判成失败 |
| **升级能沿链接覆盖项目目录外的文件** | 清理旧代码时跟着符号链接/硬链接走，写到 Skill 目录之外 |
| **升级中断会丢源文件** | 移动到备份目录的动作半途失败时，可能源已删、目标没写成 |
| **中文、空格、特殊字符路径生成无效定时配置** | `render_plist` 是字符串拼接，路径里的特殊字符没有 XML 转义 → launchd 静默不加载 |
| **23 点被算成 24 点** | 深夜时刻的边界计算溢出 |
| **看门狗无法安全更新自身** | 自更新失败或原地覆盖时留下半个脚本，监控器从此不可执行 |
| **损坏状态、异常计数、未来时间让看门狗崩溃或长期静默** | 状态文件被写坏或时钟回拨后，监控器要么崩、要么再也不告警 |
| **自动化测试会触发真实系统通知** | 独立副本的测试用例误触真实告警通道 |

### 怎么修的

- **锁**：不再用 `O_CREAT|O_EXCL` 直接建目标文件，改为
  **先把内容完整写进同目录临时文件（含 `fsync`），再用 `os.link` 发布到目标路径**。
  `os.link` 同样不覆盖已存在的目标，互斥性不变，但**目标一旦可见就已经有完整内容**——
  0 字节窗口从根上不存在了，不需要再靠 mtime 或年龄去猜
- 新增 `.run.lock.guard` + `flock`，把锁的检查、抢占、释放串行化；进程退出时内核自动解锁
- 探针文件、状态文件各用独立临时文件，并发时不再互相覆盖
- 升级清理拒绝跟随链接，移动改为可回退的两段式
- plist 生成加 XML 转义

### 测试

**305 → 325**，全量连跑三轮稳定。在中文、空格路径下完成了 WorkBuddy 隔离安装与离线自检。

## 0.3.0-rc2 · 2026-08-03

**这一批全在监控链路自身。** 上一版交付了 watchdog，但它有五处毛病，
合起来是同一个问题：**监控器比被监控对象更脆弱，等于没有监控。**
判定逻辑与业务规则一行没动。

### 🔴 业务能感知的一处变化

**周末不再推送。** 定时从 `0 9 * * *` 改成 `0 9 * * 1-5`。

README 一直写的是周一至周五，而 `install.sh` 和生产上实际跑的是每天——
三处口径不一，照着不同文档装的人会得到不同的行为，且没有任何东西会报错。
以 README 为准统一。

停滞天数按自然日算，周末不推不会让数字失真；而周末推等于教业务周末忽略它，
长期反而伤害降噪。watchdog 的判定本来就按工作日数班次，两边现在对齐了。

### 修的七件事

| 问题 | 原来会怎样 |
|---|---|
| **抢锁标记残留会永久堵死运行** | 进程在抢陈旧锁半路被强杀，标记留在盘上；锁没换 → holder token 不变 → 下次算出同一个标记名 → 建不上 → **每天静默跳过，永远**。而当时的清理函数只在「抢到标记之后」跑，正好够不着自己造成的死锁。实测连试三次三次被堵死，四进程并发时四个全被堵 |
| **告警失败反而进了去重** | `last_alert_at`/`last_alert_key` 无条件写 → 告警发失败照样进 24 小时去重 → 下次「已告警过，不重复」→ **故障还在，告警永远发不出去**。监控器自己哑了，还以为自己在响 |
| **本机通知失败被当成成功** | `osascript` 的退出码根本没看，无条件 `return True`。launchd 里没有 GUI 会话、通知权限被拒都返回非零 —— 三级降级的第三级永远走不到 |
| **配置写错直接崩** | `schedule_hour: "九"` → ValueError 当场崩；`25` 更隐蔽，要到 `time(25,0)` 才炸。**监控器崩掉 = 完全没有监控**，而原因只是一个打错的数字 |
| **监控器住在被监控对象的目录里** | Skill 被移动/删除/`hermes skills update` 装坏 → plist 指向的文件没了 → launchd 静默失败。**而「Skill 坏了」正是它最该报警的场景** |
| **升级留下会执行的旧代码** | `copy_package` 是纯合并（原注释：「不删除目标目录中的任何文件」），上一版的 `.py` 升级后仍可 import、仍会被 `unittest discover` 跑到。两个版本混在一个目录里执行 |
| **plist 硬编码 `/usr/bin/python3`** | 该文件在精简系统、Linux、CommandLineTools 没装的机器上并不存在 |

### 怎么修的

- **锁**：清理提到 `acquire_lock()` 入口（不再只在成功路径的 finally 里）；
  新增 `_claim_steal_right()`，标记超过 120 秒判为残留、清掉后重试一次。
  新鲜标记仍然让路——自愈不能把互斥一起愈掉
- **告警去重**：去重键**只在发成功后**才写；失败写 `last_alert_failed_at` /
  `alert_failures`，这两个不参与去重判定
- **本机通知**：检查 `returncode`，非零把 stderr 摘要计入失败原因并降到第三级
- **配置**：新增 `validate_config()`，逐字段校验类型与范围；坏值退回默认 +
  一条人话警告（说清哪个字段、写的什么、按什么值跑），**绝不罢工**——
  监控器因为配置坏了而不跑，正是最不该发生的事
- **独立安装**：`watchdog.py --install` 把自己复制到 `<运行时目录>/watchdog/`
  并生成路径已填好的 plist；`setup.sh` 每次安装/升级自动调用，副本跟着版本走。
  **不执行 `launchctl load`**——装不装由人决定，只打印命令
- **Python 路径**：安装时按 `/usr/bin/python3` → Homebrew → PATH 顺序检测。
  刻意不用 `sys.executable`（那可能是 hermes 的 venv，一旦依赖它，
  hermes 装坏监控器就跟着哑）
- **升级清理**：清单内的残留移进 `.upgrade-backup-<时间戳>/`——
  以 `.` 开头且不在清单里，import 和 discover 都够不着。
  **移动不是删除**，升级出问题还能翻回来。范围严格限定在 `_manifest` 清单内，
  运行时数据（`followup/` `state/` `.env`）和清单外的东西（`notes/`、
  `CHANGELOG.md`）一律不碰

### 顺带抓到的第八个问题（不在清单里，是新写的并发测试逼出来的）

**两个进程可以同时拿到运行锁。** 这不是残留标记那条，是另一个更早的洞，
一直存在，只是从没被抓到过——四进程 15 轮的测试在负载高时第 11 轮报出「2 个赢家」。

根因在锁的**创建过程本身不是原子的**：

```
_create_lock_exclusive = os.open(O_EXCL) → os.write
                                        ↑
                          这两个系统调用之间，锁文件是 0 字节
```

而 0 字节 parse 出来是 `{}`，与「锁文件损坏」完全无法区分：

```
A: open(O_EXCL) 成功，还没 write
  B: open 失败 → 读到 0 字节 → 判成「损坏的陈旧锁」→ 夺走
    → A 的 fd 指向已被 os.replace 掉的旧 inode，A 仍以为自己持锁
      → 两个进程同时在跑：并行写状态、并行推送
```

**修法**：合法的废弃锁一定有内容，所以空文件只可能是「创建者正在写」——
读侧遇到空锁一律让路，绝不夺。有内容但解析不了的锁仍然照夺（那才是真损坏）。
另外补了两处：锁在读取前被释放时重新走 O_EXCL 而不是去夺一把不存在的锁；
夺锁前的校验改为比原始字节而不是比 token（token 为 None 时 `!=` 会判成相等）。

新增三条确定性回归（不靠并发碰运气）：半创建态的锁不许夺、纯空白同理、
有内容的损坏锁仍可夺。全量测试连跑 5 遍稳定。

### 测试

243 → **304**（新增 61 条，每个问题都有对应回归）。新增文件：
`test_upgrade.py`、`test_watchdog_install.py`、`test_schedule.py`。

`run_tests.sh` 加了 `-W error::ResourceWarning`——未关闭的文件句柄从此直接判失败，
不再靠人盯输出（同时修掉了 `test_watchdog.py` 里那处 `open().read()`）。

**每条回归都在旧代码上验过会失败**，不是写完就算：
抢锁那条在旧代码上四个进程全被堵死；去重那条报「第二次被去重吃掉了」；
`osascript` 那条报「退出码 1 仍被当成告警成功」。

## 0.3.0-rc1 · 2026-08-01

**这一批全是可靠性工程，判定逻辑一行没动。** 主题只有一个：
**该出声的时候要出声。**

### 🔴 业务能感知的一处变化

**当天没有要催的项目时，本地不再完全静默。**
以前 stdout 是空的，宿主把这次运行记成 `[SILENT]`，业务手动点一下看到的就是
「毫无反应」—— 而那和「今天压根没跑起来」长得一模一样。
现在必留一行：`✅ 检查完成：读取 101 项，待催 0 项，静默期 23 项，企微未发送。`
**企微仍然一条不发**，变的只是本地这行字。

### 组3 health.json 自身损坏 + 本地回执

`health.json` 以前第一次被碰到是在 `update_health()` 内部，而那里把异常吞成
一行 stderr —— 于是**健康记录自己骨折了，反而是最没人发现的那种坏法**，
还会被静默重建，连「上次成功是什么时候」都一起丢掉。而那正是唯一能发现
「根本没跑」的线索。

现在它第一个被读，与其余状态文件共用「登记 → 隔离保留 → 告警 → 记 `last_recovery`」，
且损坏的那一次**不写 `last_full_success`**，不许伪装成完整成功。
新增 `health.last_run_summary`，`doctor` 展示它。

### 组4 运行锁 owner token + 原子抢占

三处并发缺陷：夺陈旧锁用 `write_text()` 不原子；锁没有身份标识、释放只按路径删
（A 的锁被 B 夺走后 A 跑完照样删掉 B 的，**并发保护归零且完全无声**）；
「存活但超 30 分钟」直接夺锁 —— 那个进程还活着，夺了就是两个进程同时写状态。

现在：uuid token + 释放前核对归属；持锁判定分五档（活着且 30min–6h 只告警不夺，
超 6h 才当僵死）；抢占动作本身互斥后再 `os.replace`。

> 抢占的原子性我做错过一次：第一版是「先 unlink 再 `O_EXCL` 重建」，
> 看着够原子了，但四进程 × 15 轮跑出来两个进程同时得手 ——
> **A 建好新锁后，B 的 unlink 会把 A 的新锁删掉。**
> 同一进程里顺序调两次 `acquire_lock` 永远测不出这个。

### 组5 外部存活监控（🛑 已交付，未安装）

关机、休眠、gateway 没起来、cron 被删 —— 这几种失败连一行 stderr 都不会产生。
`doctor` 查得到但要人手动跑；挂进 hermes cron 又是循环依赖。
所以执行者是 **launchd**，告警走 `hermes send`（bot-token 平台直连 REST，
**不需要 gateway 在跑**），失败退到本机通知，再失败写日志并非零退出。

判定用「数错过了几次本该执行的 9:00」而不是小时阈值 ——
工作日不关机、周末可能关机的作息下，48 小时会让周四的故障潜伏到周末，
60 小时又会在周一早上误报。数班次并跳过周末，两种情况都对。

🛑 **在实际装上 launchd 并跑过 `--self-test` 之前，这仍然只是能力，不是闭环。**

### 四类消息，别混

| 类别 | 去哪 | 何时 | 无事发生时 |
|---|---|---|---|
| **企微业务消息** | 业务群 | 有待催项目 | **不发**（无事不发） |
| **本地运行回执** | stdout → 宿主日志 | **每次真实运行** | 仍打一行摘要 ← 本版新增 |
| **故障告警** | telegram | 取数/推送/状态损坏/配置故障 | 不发 |
| **外部存活监控** | telegram + 本机通知 | 连续错过应跑班次 | 不发 ← 本版新增 |

前三类都要**任务跑起来**才会产生。第四类是唯一在任务压根没跑时还能出声的。

### 不动的部分

判定逻辑、只读白名单、业务规则、cron 的时刻与投递目标、提醒事项写入开关。
`SKILL.md` 的 frontmatter 保持 `0.2.0-rc2` 锁定的「只有 name + description」——
版本号放 `VERSION` 文件，不塞进 frontmatter。


## 2026-07-31 · 0.2.0-rc2 安装与业务交付修复

- 修复 UTF-8 locale 下 Bash 把中文标点误当成变量名一部分、导致 setup 直接退出的问题。
- Hermes 安装不再吞掉 setup 的非零退出码；失败时不生成新的 cron 启动器。
- 已有 `.env` 内容保持不变，权限统一收紧为 `600`；目录或符号链接会被拒绝。
- 新增 `bootstrap.py`，用固定参数安装到 WorkBuddy 当前工作空间或 Hermes。
- README 顶部提供业务唯一指令；其他 WorkBuddy 文档不再复制竞争提示词。
- Skill frontmatter 只保留标准字段，通用路径与命令统一使用 `FOLLOWUP_HOME`。
- 新增零依赖 Release 构建、SHA-256 校验和成品压缩包隔离安装测试。

## 2026-07-31 · 停滞天数改为超期天数（业务提出）

**推送里的数字换了含义。** 原来显示「在这个节点待了多久」，现在显示「超出允许期多久」。

| 阶段 | 允许 | 举例：某项目在节点 179 天 |
|---|---|---|
| ①收资 | 6 天 | — |
| ②专家评估 | 3 天 | — |
| ③预调试/安装 | 21 天 | — |
| ④节能测试 | 21 天 | 旧：`停滞 179 天` → 新：**`超期 158 天`** |

**为什么改**：业务原话「安装日+21天才开始启动提醒，超出 21 天才开始算停滞日期」。
她的理由比字面更强 —— 旧口径下「节能测试 24 天」和「待收资 29 天」看着差不多，
实际一个超期 3 天、一个超期 22 天。**同一个数字在不同阶段含义完全不同，没法横向比较。**

**改了什么、没改什么**：

- ✅ **判定一行没动**。`stalled_days` 仍是「在节点待了多久」，`is_overdue()` 仍用它。
  新增的 `overdue_days` 是派生属性，只用于显示 —— 判定逻辑是这个工具唯一不能出错的
  部分，不该为了改个数字去碰它
- ✅ **实测验证**：改动前后各跑一次 `--today 2026-08-07 --json`，
  **28 条待催的企业名、节点、在节点天数逐条完全一致**
- 允许天数取 `首次提醒日 − 1` 而不是 `threshold.days`：保证项目一进清单，
  超期天数必定 ≥ 1。①收资 是 `boundary=on`，按后者算首推那天正好是 0，
  一条刚触发的催办显示「超期 0 天」会像 bug
- `--json` **两个数都给**（`stalled_days` / `allowance_days` / `overdue_days`），
  排查时仍拿得到真实停留天数
- 提醒事项标题用超期天数，**备注里两个都留** —— 那是她查细节的地方

**🔴 业务能感知的两处变化**：

1. 「超2个月 可考虑终止」分割线改按超期天数算，**比原来晚 6 天出现**
   （待收资需超期 60 天 = 在节点 66 天）
2. 🟠 **③④ 的时钟起点本身是近似值**——台账没有「安装成功日期」这一列，
   程序用快照推出的节点变更日，存量项目回退到「最新进展日期」。
   旧口径下这个误差无所谓（179 天差几天不影响判断），**新口径把它推到台前**：
   「超期 3 天」若起点差 5 天，这个数字就是错的。
   `stage_entered` 跑满 1–2 个月后会自然收敛，在那之前小数字要当估值看

## 2026-07-31 · 加固（P0/P1/P2）+ 输出格式改版

这一轮**没有加功能**，做的是让「坏了」这件事必须可见、可回滚、有测试守住。

修之前有一类共同的病：**出了事看起来像没事**。企微推没出去、告警发不出去、
序号填错、项目返工回退——四种失败全都表现成「今天很安静」，而那和
「今天没有超时单」长得一模一样。

---

### 🔴 业务能感知的两处行为变化

| 变化 | 说明 |
|---|---|
| **企微推送部分失败时，下次会把整批重发** | 一条消息装多个项目，无法知道具体哪几条送到了对方眼前，所以整批不提交状态。业务可能看到一小段重复。**这是刻意的取舍**：重复消息她一眼能识别并忽略，静默漏催她永远发现不了 |
| **①收资 提前一天触发** | 原文是「自上报日起满1周应提醒」，旧实现要到第 8 天。已按原文校准为**第 7 天**。②③④⑤ 原文是「N 天内没有则提醒」，维持第 N+1 天不变。<br>实测未来 8 天内**零差异**——只有恰好卡在第 7 天的收资项目才受影响 |

---

### P0-A：推送失败不得记为已通知

**病灶**：`last_notified` 无条件写入，与推送结果无关；脚本恒返回 0，Hermes 显示成功。
后果是业务没收到消息，系统却认为已通知，7 天内静默。

**修法**——把两类性质完全不同的状态拆开：

| 状态 | 性质 | 何时写 |
|---|---|---|
| `stage_entered` / `stage_history` / `snapshot_*` / `first_overdue` | 事实观测，与有没有通知无关 | 每次真实运行都写 |
| `followup_state[*].last_notified` | 投递凭证 | **仅当主通道完整送达** |

- `output.json` 新增 `notify.primary` 显式声明主通知通道
- `wecom_push.push()` 改为返回 `PushResult`，区分「没试」「失败」「成功」三种，不再用裸 bool 把它们糊在一起
- 退出码：`0` 正常 / `1` 主任务失败 / `2` 启动阶段故障
- `--dry-run`、`--json`、`--today`（不带 `--force-push`）**一律不发不写**。`--today` 从此是纯模拟
- `--today` 是未来日期时禁止 `--force-push`——那会写出未来日期的快照，把计时基准彻底污染

**兼容性**：`notify.primary` 缺省为 `wecom_webhook`；若它指向的通道 `enabled=false`，启动即失败（退出码 2），而不是默默地永远提交不了状态。

### P0-B：故障告警不得静默失败

**病灶**三处：不看 `returncode`；`telegram:<chat_id>` 硬编码在代码里；找不到 `hermes` 时被 `except` 吞掉。

- 告警目标移到 `.env` 的 `FOLLOWUP_ALERT_TARGET`，**代码里不再有任何个人 ID**
- `hermes` 可执行文件显式查找（`shutil.which` + 回退路径），找不到即报错
- 检查退出码、stderr、超时，返回 `(ok, detail)`，调用方必须处理
- 发不出去时把**原本要告警的内容**打进 stderr，至少在 hermes 日志里留痕
- 统一故障出口 `fail()`：配置读取、无启用台账、节假日表损坏、状态目录不可写、主通道错配，全部走同一条路径（stderr + health + 告警 + 非零）
- **「告警发出去了」≠「主任务成功」**：告警结果只记进 health，绝不参与退出码计算

**新增 `state/health.json`**，记录最近成功取数 / 成功推送 / 完整成功 / 失败原因 / 连续失败次数。
它抓的是别的机制都抓不到的那类失败——**根本没跑**（关机、休眠、gateway 没起来、cron 被删）。
这类失败连 stderr 都不会产生。`doctor` 会在超过 48 小时没成功时报 🔴。

### P1-A：人工名单身份校验

**病灶**：`manual_terminal` / `terminal_note_exceptions` / `paused` / 灰名单全靠序号匹配，
序号填错或被新项目复用会永久排除错误项目，自检也不报警。而且后三张名单配置里**根本没有企业名**。

- `ledgers.json` 给例外表（4 条）、暂缓（2 条）补 `name`；灰名单由字符串数组改为 `[{key,name}]`（29 条）
- `core.check_manual_lists()` 统一校验全部名单，**一处实现四张共用**，不复制逻辑
- 序号不存在 / 名称对不上 → 致命；灰名单 → 警告（它不参与催与不催，为一条复核提示停掉整个催办不成比例）
- 缺 `name` 的条目 → 警告提示补齐，不报错（不能让一次格式升级把正在跑的催办打断）

**兼容性**：旧的纯字符串数组仍能跑。**本次未删除任何现有记录。**
台账正式加「终止」列后，`manual_terminal` 应逐条核对后删除。

### P1-B：节点回退开新计时周期

**病灶**：`stage_entered` 只 `setdefault` 从不清理。A→B 之后返工退回 A，直接命中几个月前的旧条目，凭空算出几百天停滞。

- 检测节点变更，关闭旧周期并写入 `state/stage_history.json`（只增不删）
- 回退时丢弃本节点的一切遗留，**包括 `followup_state`**
- 起点口径（业务 2026-07-31 确认）：**「最新进展日期」，但不早于上次快照日**——上次快照那天它确知还在别的节点
- 状态文件损坏：坏文件**改名保留**（`*.corrupt.<时间戳>`，不删）+ 进报告 + 记 health + 触发告警

> ⚠️ 实现时踩过一次坑，值得记下来：第一版只检查 `stage_entered` 里有没有残留，
> 但那个键在上一次节点变更时就已经被摘走了。真正活到现在的是 `followup_state`——
> 它只在「未超期」时才被清。不清它，回退后的新周期一进来就被上一轮的复提醒间隔
> 静默掉，**返工之后反而不催了**。是 `test_rollback_clears_stale_notification_record`
> 抓出来的。

**副产品**：`stage_history.json` 就是方案里说「要跑 1~2 个月才有样本」的阶段耗时数据，从现在开始自动积累。

### P1-C：阈值边界

- 每个节点新增 `threshold.boundary`：`on` = 满 N 天当天提醒，`after` = N 天内没有才提醒
- 缺省 `after`（＝历史行为），**新增节点不会因为忘填而静默改口径**；写错值直接报错
- `doctor` 新增「第几天首次提醒」对照表——这是给业务看的口径凭证

### P2-A：消息拆分硬保证

两个洞都实测复现过：单行 4500 字节原样返回；`（1/2）` 前缀拆完才加导致每片 4009 字节。

- 三级拆分：空行 → 行 → 安全硬拆（优先在标点断，按码点不按字节，断点补「…」）
- 两遍法把前缀计入限额
- 最终断言每片 ≤ `min(split_bytes, 4096)`，不过则抛异常走失败路径——**宁可整批失败并告警，也不发一条会被企微拒收的消息**

### 并发保护

`state/.run.lock`。持锁进程存活且未超 30 分钟 → 安静跳过（退出码 0，不是故障）；
陈旧锁夺回并告警——**否则一次卡死会让之后每天都静默跳过**。`--dry-run` / `--json` 不加锁。

---

### 输出格式改版（业务 2026-07-31 提出）

| 改动 | 说明 |
|---|---|
| 排序改**升序** | 停滞短的在前 |
| 组内编号 | `1、2、3、` |
| 「超2个月 可考虑终止」分割线 | **只在【待收资】**，插在第一个 ≥60 天的项目之前 |
| 阶段改名 | 收资→**待收资**、专家评估→**待专家评估**；预调试/安装、节能测试不变 |
| 去掉台账序号 | 业务本人改口（上午还要求「标注台账序号方便查找」）。⚠️ 代价：她想在 101 行台账里定位某条时只能搜企业名 |
| 去掉阶段描述与积压注释 | 「催客户/客户经理交资料」这类全删，减少信息长度。配置里的 `action` 保留，仍用于提醒事项备注 |
| 只有一个阶段时不显示「积压最重」 | 那必然是 100%，等于没说 |

**样式**：只做标题分级（`##` 业务线、`###` 阶段），**条目一律不加样式**。

- 「小一号字」做不到——官方文档原文：**markdown_v2 不支持字体颜色**。旧版 markdown 有颜色但不支持列表，会退回业务投诉过的「一堵墙」
- 我曾提议「反过来抬高还有救的项（加粗）」，**业务否掉了**

两处刻意的语法选择：

1. **序号用「1、」而不是「1.」**——markdown 的有序列表被分割线打断后，后半段可能被渲染器重新从 1 编号，业务会看到两个「1」。顿号是纯文本，零渲染风险
2. **分割线写成 `------- 文字 -------` 整行**，不用单独的 `---`——紧跟在文字后的一行 `---` 在 markdown 里会把上一行变成二级标题

两套渲染（终端 / 企微）共用 `group_items()`，测试强制两边条目与顺序完全一致——防止只改了一边。

---

### 测试与工程

- 新增 `tests/`，纯标准库 `unittest`（**用例数以 `./run_tests.sh` 的实际输出为准**——
  写死在文档里的计数注定过期，这里原本写的 124 就已经不对了）
- **零网络零真实状态**：`HERMES_HOME` 指向临时目录，腾讯文档 / 企微 / telegram / 提醒事项全部打桩
- `run_tests.sh` 一键跑
- `core.CONFIG_DIR` / `STATE_DIR` 由模块常量改为 `config_dir()` / `state_dir()` 函数——常量在 import 那一刻就定死了，测试没法切目录

跑法：

```bash
cd ~/.hermes/skills/work/followup-genie && ./run_tests.sh
```

---

### 不变的部分

判定逻辑、只读白名单、业务规则（节点/触发条件/责任范围/终止判定优先级）、
cron 的时刻与投递目标、提醒事项写入开关（开发机上始终 `false`）。

**本次未删除任何文件、未删除任何配置记录。**
