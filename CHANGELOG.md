# 变更记录

## 0.3.0-rc1 · 2026-08-01

**这一批全是可靠性工程，判定逻辑一行没动。** 主题只有一个：
**该出声的时候要出声。**

### 🔴 业务能感知的一处变化

**当天没有要催的项目时，本地不再完全静默。**
以前 stdout 是空的，宿主把这次运行记成 `[SILENT]`，业务手动点一下看到的就是
「毫无反应」—— 而那和「今天压根没跑起来」长得一模一样。
现在必留一行：`✅ 检查完成：读取 101 项，待催 0 项，静默期 23 项，企微未发送。`
**企微仍然一条不发**，变的只是本地这行字。

### 组3 health.json 自身损坏 + 本地回执

`health.json` 以前第一次被碰到是在 `update_health()` 内部，而那里把异常吞成
一行 stderr —— 于是**健康记录自己骨折了，反而是最没人发现的那种坏法**，
还会被静默重建，连「上次成功是什么时候」都一起丢掉。而那正是唯一能发现
「根本没跑」的线索。

现在它第一个被读，与其余状态文件共用「登记 → 隔离保留 → 告警 → 记 `last_recovery`」，
且损坏的那一次**不写 `last_full_success`**，不许伪装成完整成功。
新增 `health.last_run_summary`，`doctor` 展示它。

### 组4 运行锁 owner token + 原子抢占

三处并发缺陷：夺陈旧锁用 `write_text()` 不原子；锁没有身份标识、释放只按路径删
（A 的锁被 B 夺走后 A 跑完照样删掉 B 的，**并发保护归零且完全无声**）；
「存活但超 30 分钟」直接夺锁 —— 那个进程还活着，夺了就是两个进程同时写状态。

现在：uuid token + 释放前核对归属；持锁判定分五档（活着且 30min–6h 只告警不夺，
超 6h 才当僵死）；抢占动作本身互斥后再 `os.replace`。

> 抢占的原子性我做错过一次：第一版是「先 unlink 再 `O_EXCL` 重建」，
> 看着够原子了，但四进程 × 15 轮跑出来两个进程同时得手 ——
> **A 建好新锁后，B 的 unlink 会把 A 的新锁删掉。**
> 同一进程里顺序调两次 `acquire_lock` 永远测不出这个。

### 组5 外部存活监控（🛑 已交付，未安装）

关机、休眠、gateway 没起来、cron 被删 —— 这几种失败连一行 stderr 都不会产生。
`doctor` 查得到但要人手动跑；挂进 hermes cron 又是循环依赖。
所以执行者是 **launchd**，告警走 `hermes send`（bot-token 平台直连 REST，
**不需要 gateway 在跑**），失败退到本机通知，再失败写日志并非零退出。

判定用「数错过了几次本该执行的 9:00」而不是小时阈值 ——
工作日不关机、周末可能关机的作息下，48 小时会让周四的故障潜伏到周末，
60 小时又会在周一早上误报。数班次并跳过周末，两种情况都对。

🛑 **在实际装上 launchd 并跑过 `--self-test` 之前，这仍然只是能力，不是闭环。**

### 四类消息，别混

| 类别 | 去哪 | 何时 | 无事发生时 |
|---|---|---|---|
| **企微业务消息** | 业务群 | 有待催项目 | **不发**（无事不发） |
| **本地运行回执** | stdout → 宿主日志 | **每次真实运行** | 仍打一行摘要 ← 本版新增 |
| **故障告警** | telegram | 取数/推送/状态损坏/配置故障 | 不发 |
| **外部存活监控** | telegram + 本机通知 | 连续错过应跑班次 | 不发 ← 本版新增 |

前三类都要**任务跑起来**才会产生。第四类是唯一在任务压根没跑时还能出声的。

### 不动的部分

判定逻辑、只读白名单、业务规则、cron 的时刻与投递目标、提醒事项写入开关。
`SKILL.md` 的 frontmatter 保持 `0.2.0-rc2` 锁定的「只有 name + description」——
版本号放 `VERSION` 文件，不塞进 frontmatter。


## 2026-07-31 · 0.2.0-rc2 安装与业务交付修复

- 修复 UTF-8 locale 下 Bash 把中文标点误当成变量名一部分、导致 setup 直接退出的问题。
- Hermes 安装不再吞掉 setup 的非零退出码；失败时不生成新的 cron 启动器。
- 已有 `.env` 内容保持不变，权限统一收紧为 `600`；目录或符号链接会被拒绝。
- 新增 `bootstrap.py`，用固定参数安装到 WorkBuddy 当前工作空间或 Hermes。
- README 顶部提供业务唯一指令；其他 WorkBuddy 文档不再复制竞争提示词。
- Skill frontmatter 只保留标准字段，通用路径与命令统一使用 `FOLLOWUP_HOME`。
- 新增零依赖 Release 构建、SHA-256 校验和成品压缩包隔离安装测试。

## 2026-07-31 · 停滞天数改为超期天数（业务提出）

**推送里的数字换了含义。** 原来显示「在这个节点待了多久」，现在显示「超出允许期多久」。

| 阶段 | 允许 | 举例：某项目在节点 179 天 |
|---|---|---|
| ①收资 | 6 天 | — |
| ②专家评估 | 3 天 | — |
| ③预调试/安装 | 21 天 | — |
| ④节能测试 | 21 天 | 旧：`停滞 179 天` → 新：**`超期 158 天`** |

**为什么改**：业务原话「安装日+21天才开始启动提醒，超出 21 天才开始算停滞日期」。
她的理由比字面更强 —— 旧口径下「节能测试 24 天」和「待收资 29 天」看着差不多，
实际一个超期 3 天、一个超期 22 天。**同一个数字在不同阶段含义完全不同，没法横向比较。**

**改了什么、没改什么**：

- ✅ **判定一行没动**。`stalled_days` 仍是「在节点待了多久」，`is_overdue()` 仍用它。
  新增的 `overdue_days` 是派生属性，只用于显示 —— 判定逻辑是这个工具唯一不能出错的
  部分，不该为了改个数字去碰它
- ✅ **实测验证**：改动前后各跑一次 `--today 2026-08-07 --json`，
  **28 条待催的企业名、节点、在节点天数逐条完全一致**
- 允许天数取 `首次提醒日 − 1` 而不是 `threshold.days`：保证项目一进清单，
  超期天数必定 ≥ 1。①收资 是 `boundary=on`，按后者算首推那天正好是 0，
  一条刚触发的催办显示「超期 0 天」会像 bug
- `--json` **两个数都给**（`stalled_days` / `allowance_days` / `overdue_days`），
  排查时仍拿得到真实停留天数
- 提醒事项标题用超期天数，**备注里两个都留** —— 那是她查细节的地方

**🔴 业务能感知的两处变化**：

1. 「超2个月 可考虑终止」分割线改按超期天数算，**比原来晚 6 天出现**
   （待收资需超期 60 天 = 在节点 66 天）
2. 🟠 **③④ 的时钟起点本身是近似值**——台账没有「安装成功日期」这一列，
   程序用快照推出的节点变更日，存量项目回退到「最新进展日期」。
   旧口径下这个误差无所谓（179 天差几天不影响判断），**新口径把它推到台前**：
   「超期 3 天」若起点差 5 天，这个数字就是错的。
   `stage_entered` 跑满 1–2 个月后会自然收敛，在那之前小数字要当估值看

## 2026-07-31 · 加固（P0/P1/P2）+ 输出格式改版

这一轮**没有加功能**，做的是让「坏了」这件事必须可见、可回滚、有测试守住。

修之前有一类共同的病：**出了事看起来像没事**。企微推没出去、告警发不出去、
序号填错、项目返工回退——四种失败全都表现成「今天很安静」，而那和
「今天没有超时单」长得一模一样。

---

### 🔴 业务能感知的两处行为变化

| 变化 | 说明 |
|---|---|
| **企微推送部分失败时，下次会把整批重发** | 一条消息装多个项目，无法知道具体哪几条送到了对方眼前，所以整批不提交状态。业务可能看到一小段重复。**这是刻意的取舍**：重复消息她一眼能识别并忽略，静默漏催她永远发现不了 |
| **①收资 提前一天触发** | 原文是「自上报日起满1周应提醒」，旧实现要到第 8 天。已按原文校准为**第 7 天**。②③④⑤ 原文是「N 天内没有则提醒」，维持第 N+1 天不变。<br>实测未来 8 天内**零差异**——只有恰好卡在第 7 天的收资项目才受影响 |

---

### P0-A：推送失败不得记为已通知

**病灶**：`last_notified` 无条件写入，与推送结果无关；脚本恒返回 0，Hermes 显示成功。
后果是业务没收到消息，系统却认为已通知，7 天内静默。

**修法**——把两类性质完全不同的状态拆开：

| 状态 | 性质 | 何时写 |
|---|---|---|
| `stage_entered` / `stage_history` / `snapshot_*` / `first_overdue` | 事实观测，与有没有通知无关 | 每次真实运行都写 |
| `followup_state[*].last_notified` | 投递凭证 | **仅当主通道完整送达** |

- `output.json` 新增 `notify.primary` 显式声明主通知通道
- `wecom_push.push()` 改为返回 `PushResult`，区分「没试」「失败」「成功」三种，不再用裸 bool 把它们糊在一起
- 退出码：`0` 正常 / `1` 主任务失败 / `2` 启动阶段故障
- `--dry-run`、`--json`、`--today`（不带 `--force-push`）**一律不发不写**。`--today` 从此是纯模拟
- `--today` 是未来日期时禁止 `--force-push`——那会写出未来日期的快照，把计时基准彻底污染

**兼容性**：`notify.primary` 缺省为 `wecom_webhook`；若它指向的通道 `enabled=false`，启动即失败（退出码 2），而不是默默地永远提交不了状态。

### P0-B：故障告警不得静默失败

**病灶**三处：不看 `returncode`；`telegram:<chat_id>` 硬编码在代码里；找不到 `hermes` 时被 `except` 吞掉。

- 告警目标移到 `.env` 的 `FOLLOWUP_ALERT_TARGET`，**代码里不再有任何个人 ID**
- `hermes` 可执行文件显式查找（`shutil.which` + 回退路径），找不到即报错
- 检查退出码、stderr、超时，返回 `(ok, detail)`，调用方必须处理
- 发不出去时把**原本要告警的内容**打进 stderr，至少在 hermes 日志里留痕
- 统一故障出口 `fail()`：配置读取、无启用台账、节假日表损坏、状态目录不可写、主通道错配，全部走同一条路径（stderr + health + 告警 + 非零）
- **「告警发出去了」≠「主任务成功」**：告警结果只记进 health，绝不参与退出码计算

**新增 `state/health.json`**，记录最近成功取数 / 成功推送 / 完整成功 / 失败原因 / 连续失败次数。
它抓的是别的机制都抓不到的那类失败——**根本没跑**（关机、休眠、gateway 没起来、cron 被删）。
这类失败连 stderr 都不会产生。`doctor` 会在超过 48 小时没成功时报 🔴。

### P1-A：人工名单身份校验

**病灶**：`manual_terminal` / `terminal_note_exceptions` / `paused` / 灰名单全靠序号匹配，
序号填错或被新项目复用会永久排除错误项目，自检也不报警。而且后三张名单配置里**根本没有企业名**。

- `ledgers.json` 给例外表（4 条）、暂缓（2 条）补 `name`；灰名单由字符串数组改为 `[{key,name}]`（29 条）
- `core.check_manual_lists()` 统一校验全部名单，**一处实现四张共用**，不复制逻辑
- 序号不存在 / 名称对不上 → 致命；灰名单 → 警告（它不参与催与不催，为一条复核提示停掉整个催办不成比例）
- 缺 `name` 的条目 → 警告提示补齐，不报错（不能让一次格式升级把正在跑的催办打断）

**兼容性**：旧的纯字符串数组仍能跑。**本次未删除任何现有记录。**
台账正式加「终止」列后，`manual_terminal` 应逐条核对后删除。

### P1-B：节点回退开新计时周期

**病灶**：`stage_entered` 只 `setdefault` 从不清理。A→B 之后返工退回 A，直接命中几个月前的旧条目，凭空算出几百天停滞。

- 检测节点变更，关闭旧周期并写入 `state/stage_history.json`（只增不删）
- 回退时丢弃本节点的一切遗留，**包括 `followup_state`**
- 起点口径（业务 2026-07-31 确认）：**「最新进展日期」，但不早于上次快照日**——上次快照那天它确知还在别的节点
- 状态文件损坏：坏文件**改名保留**（`*.corrupt.<时间戳>`，不删）+ 进报告 + 记 health + 触发告警

> ⚠️ 实现时踩过一次坑，值得记下来：第一版只检查 `stage_entered` 里有没有残留，
> 但那个键在上一次节点变更时就已经被摘走了。真正活到现在的是 `followup_state`——
> 它只在「未超期」时才被清。不清它，回退后的新周期一进来就被上一轮的复提醒间隔
> 静默掉，**返工之后反而不催了**。是 `test_rollback_clears_stale_notification_record`
> 抓出来的。

**副产品**：`stage_history.json` 就是方案里说「要跑 1~2 个月才有样本」的阶段耗时数据，从现在开始自动积累。

### P1-C：阈值边界

- 每个节点新增 `threshold.boundary`：`on` = 满 N 天当天提醒，`after` = N 天内没有才提醒
- 缺省 `after`（＝历史行为），**新增节点不会因为忘填而静默改口径**；写错值直接报错
- `doctor` 新增「第几天首次提醒」对照表——这是给业务看的口径凭证

### P2-A：消息拆分硬保证

两个洞都实测复现过：单行 4500 字节原样返回；`（1/2）` 前缀拆完才加导致每片 4009 字节。

- 三级拆分：空行 → 行 → 安全硬拆（优先在标点断，按码点不按字节，断点补「…」）
- 两遍法把前缀计入限额
- 最终断言每片 ≤ `min(split_bytes, 4096)`，不过则抛异常走失败路径——**宁可整批失败并告警，也不发一条会被企微拒收的消息**

### 并发保护

`state/.run.lock`。持锁进程存活且未超 30 分钟 → 安静跳过（退出码 0，不是故障）；
陈旧锁夺回并告警——**否则一次卡死会让之后每天都静默跳过**。`--dry-run` / `--json` 不加锁。

---

### 输出格式改版（业务 2026-07-31 提出）

| 改动 | 说明 |
|---|---|
| 排序改**升序** | 停滞短的在前 |
| 组内编号 | `1、2、3、` |
| 「超2个月 可考虑终止」分割线 | **只在【待收资】**，插在第一个 ≥60 天的项目之前 |
| 阶段改名 | 收资→**待收资**、专家评估→**待专家评估**；预调试/安装、节能测试不变 |
| 去掉台账序号 | 业务本人改口（上午还要求「标注台账序号方便查找」）。⚠️ 代价：她想在 101 行台账里定位某条时只能搜企业名 |
| 去掉阶段描述与积压注释 | 「催客户/客户经理交资料」这类全删，减少信息长度。配置里的 `action` 保留，仍用于提醒事项备注 |
| 只有一个阶段时不显示「积压最重」 | 那必然是 100%，等于没说 |

**样式**：只做标题分级（`##` 业务线、`###` 阶段），**条目一律不加样式**。

- 「小一号字」做不到——官方文档原文：**markdown_v2 不支持字体颜色**。旧版 markdown 有颜色但不支持列表，会退回业务投诉过的「一堵墙」
- 我曾提议「反过来抬高还有救的项（加粗）」，**业务否掉了**

两处刻意的语法选择：

1. **序号用「1、」而不是「1.」**——markdown 的有序列表被分割线打断后，后半段可能被渲染器重新从 1 编号，业务会看到两个「1」。顿号是纯文本，零渲染风险
2. **分割线写成 `------- 文字 -------` 整行**，不用单独的 `---`——紧跟在文字后的一行 `---` 在 markdown 里会把上一行变成二级标题

两套渲染（终端 / 企微）共用 `group_items()`，测试强制两边条目与顺序完全一致——防止只改了一边。

---

### 测试与工程

- 新增 `tests/`，纯标准库 `unittest`（**用例数以 `./run_tests.sh` 的实际输出为准**——
  写死在文档里的计数注定过期，这里原本写的 124 就已经不对了）
- **零网络零真实状态**：`HERMES_HOME` 指向临时目录，腾讯文档 / 企微 / telegram / 提醒事项全部打桩
- `run_tests.sh` 一键跑
- `core.CONFIG_DIR` / `STATE_DIR` 由模块常量改为 `config_dir()` / `state_dir()` 函数——常量在 import 那一刻就定死了，测试没法切目录

跑法：

```bash
cd ~/.hermes/skills/work/followup-genie && ./run_tests.sh
```

---

### 不变的部分

判定逻辑、只读白名单、业务规则（节点/触发条件/责任范围/终止判定优先级）、
cron 的时刻与投递目标、提醒事项写入开关（开发机上始终 `false`）。

**本次未删除任何文件、未删除任何配置记录。**
