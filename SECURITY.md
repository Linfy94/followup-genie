# 安全说明

## 数据边界

本项目的最高优先级规则是：项目台账只读。

- 腾讯文档调用只允许读取工作表列表、表结构、表格数据和文档元数据。
- 会修改表格的接口不在代码白名单中，也不能由业务配置打开。
- 催办状态只保存在本地运行目录，不回写台账。
- 企业微信 Webhook 和腾讯文档凭证只放在运行目录的 `.env` 中。

## 禁止提交到 Git

- `.env`
- 真实 `config/` 与 `state/`
- 客户或项目清单
- 腾讯文档链接、token、企业微信 Webhook
- WorkBuddy 或 Hermes 的运行日志

本仓库中的模板都是虚构示例。发布前仍应再次运行敏感信息检查。

## WorkBuddy 权限建议

使用默认权限模式。首次运行脚本、访问网络或写本地状态时，WorkBuddy 可能要求确认，
这是正常的安全机制。只允许本 Skill 访问单独的业务测试工作空间，不要把整个桌面、
下载目录或个人主目录设为工作空间。

## 发现问题时

如果怀疑凭证泄漏，先在腾讯文档开放平台和企业微信中立即撤销或更换凭证，再保存日志
和复现时间。提交问题时只提供脱敏日志，不粘贴完整 token 或 Webhook。
