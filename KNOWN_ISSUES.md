# 已知问题

本文件对应 `1.1.1`。Hermes 与 WorkBuddy 两条路径已经业务实际使用验证；
下面列的是仍然存在、需要留意的边界（如 Windows 未验证），不代表全部风险已清零。

## 业务测试前必须知道

1. ~~**WorkBuddy 尚未做端到端实机验证。** 官方文档确认能上传本地技能包，但没有公开~~
   ~~完整的包格式细节；本次同时提供 `.skill` 和 `.zip` 供实测。~~

   **✅ 已由业务实机验证。** 业务老师在 `0.6.0` 系列期间一直用 WorkBuddy
   配置和运行项目跟进精灵，端到端实机路径已经跑通，不再是未验证状态。
2. **WorkBuddy 定时运行会发起 Agent 任务。** 与 Hermes 的 `--no-agent` 不同，
   会消耗相应积分，也受模型、权限确认和客户端在线状态影响。
3. **Windows 尚未验证。** 当前安装脚本使用 Bash，macOS 提醒事项依赖 AppleScript。
   Windows 只能作为后续适配目标，不能列入本轮验收。
4. **外部存活监控：开发机上已闭环，业务电脑上仍需各自装一次。**
   `0.3.0-rc2` 起 `setup.sh` 会自动把监控器复制到 `<运行时目录>/watchdog/`
   并生成一份路径已填好的 plist（Python 路径按安装时检测结果写入）——
   它因此**活在 Skill 之外**，Skill 被移动、删除或升级装坏都影响不到它。

   ✅ 2026-08-03 在开发机上完成闭环：launchd 已装载、`--self-test`
   真实发出并收到告警消息。**这是「能发现任务根本没跑」第一次不再只是能力。**

   ⚠️ 但这个闭环**不会随包分发**——launchd 装载和 `--self-test` 是每台机器
   各自的动作。业务电脑上装完之后必须重跑 `docs/外部监控.md` 里那两条
   launchctl 命令并验一次告警，否则那台机器上电脑休眠、调度器没起来、
   定时任务被删这几种情况仍然完全无声。
5. ~~**调休补班日收不到催办。**~~ **已在 0.4.0-rc1 修复。**
   2026 有 6 个补班日（1/4、2/14、2/28、5/9、9/20、10/10），业务那天在上班，
   而当时的定时表达式把星期字段写死成周一至周五，周六周日那几天**根本不触发**，
   脚本连判断的机会都没有 —— 表现和「今天没有要催的」一模一样。

   0.3.0-rc4 当时是刻意选的取舍（不动生产定时任务，节假日表配错时最多退回
   照推、不会反过来在周末推）。0.4.0-rc1 改成 **cron 每天叫醒一次、
   由脚本按 `config/holidays.json` 判断**：普通周末不发、法定假日不发、
   补班日照发。三种情形收敛到 `WorkdayCalc.is_workday()` 一处。

   🔴 **随之而来的新代价，装机时必须注意**：节假日表漏拷或
   `workday.exclude_holidays` 为 false 时，法定假日会**照发**、补班日会**不发**。
   以前 cron 兜着周末，现在没有了。`doctor` 会就此点名报警。


6. **安装只走一个入口：`scripts/bootstrap.py`。**
   它会按宿主自动调 `setup.sh`（通用引导）或 `install.sh`（Hermes 额外写 cron 启动器）。
   那两个脚本也能单独跑，但只适用于「代码已经就位、只想重建配置」——
   正常安装直接调它们会绕过代码分发那一步。
7. ~~**不支持企业微信文档台账。** 客户端手工创建的表拿不到可供接口调用的~~
   ~~`docid`；企业模式下机器人也不继承成员对已有文档的权限，实测读取返回~~
   ~~`851003`。这不是少填一个凭证，而是当前授权模型无法完成只读接入。~~
   ~~本轮不实现企业微信文档适配层。~~

   **✅ 已在 0.4.0-rc10 接入。** 上面两条判断实测都不成立：
   `wecom-cli doc` 的读接口**都收 `url`**，不需要 `docid`；返回的也不是
   `851003` 而是 `851008 partial no authorization` —— 那是机器人缺
   **「获取成员文档内容」这一项能力授权**，在企微「工作台 → 智能机器人」
   一次开通即对全部文档生效，不是每份单独授权。授权 2026-08-10 通过后当天接入。

   ⚠️ **别把这句和 `851003` 搞混，那是两层。** 上面说的「一次开通」指的是
   **能力授权**（`851008` 那一层）。`docs/企业微信文档接入.md` 里还列了
   `851003`＝机器人对**某一份文档**没有对象权限，那是另一层、需要单份处理。
   本项目**从未实际遇到过 `851003`**（探路时预期是它、实际返回 `851008`），
   那一行在接入文档里已标「未实测」。

   🔴 **仍然未知：这项授权会不会过期。** 只能观察若干天。
   防护是「非零 `errcode` 一律抛错」—— 授权失效时会明确报故障、退出码非 0，
   不会表现成「今天没有要催的」。

   ✅ **2026-08-21 实测答案：会过期。** 部署 `0.6.0-rc17→rc20` 期间，6 份
   企微文档台账（舆情/AI体检/GEO 的前期与主台账）真的报过
   `851014 authorization expired`——doctor 与同刻新旧代码对比都按设计
   正确报成故障，没有伪装成「今天没有要催的」。当天内业务重新授权后
   恢复正常。**触发周期/时长仍不清楚**，只确认了「会过期」与「防护
   按预期起作用」这两件事，业务电脑上首次遇到时仍需按
   `docs/企业微信文档接入.md` 的错误对照表处理。

8. **企微文档台账做不了只读性验证。** `sheet_get_info` 的返回里
   **没有修改时间**（顶层只有 `errcode/errmsg/name/sheets/url`），
   所以无法像腾讯文档那样跑完比对一次 `last_modify_time`。
   与飞书同样，只能靠 `wecom_doc.ALLOWED_SUBCOMMANDS` 这道命令白名单兜底
   —— 白名单里只有 `sheet_get_info` 和 `get_doc_content` 两个只读命令。

## 已在 `1.1.1` 修复

**外部审查发现的 2 个 P0 + 1 个 P1，均已复现修复，详见 CHANGELOG.md：**

- **迁移中断误判**：`bootstrap.py` 的 `--recover-only` 返回 0 时没检查
  完成标记，会把"迁移已完整写完、只是清理阶段被强杀"误判成"从没碰过
  状态"，错误回滚代码。现在会额外看 `migrations_completed.json` 有没有
  落盘。
- **WorkBuddy 上关键数据质量问题会彻底无人知晓**：1.1.0 把"整列失效"
  这类会导致漏催的关键问题也一刀切搬去 telegram，而 telegram 依赖
  hermes send，WorkBuddy 装机没有 hermes。现在按 🔴 前缀区分"关键"和
  "温和"两级，telegram 发不出去时关键问题会退到企微发一条简短摘要
  兜底；两个通道都没送达会让 `exit_code` 顶成 1。`FOLLOWUP_ALERT_TARGET`
  在 README 里也不再写成单纯"可选"。
- **数据质量通知拖慢企微业务推送**：telegram 重试最坏累计 97 秒，
  原来排在企微推送之前会拖慢业务真正在等的清单。现在企微先发。

## 已在 `1.1.0` 改动

**数据质量诊断信息（known_values 出现未知取值这类）不再进企微推送，
改走 telegram。** 业务反馈演示时看到一整段已知值枚举观感很差，这些
内容本来是给排查用的。诊断信息一个字都没丢——终端 / `--verbose` /
`--json` / `doctor.py` 四处原样保留，只是不再重复推进企微。telegram
走的是已有的 `FOLLOWUP_ALERT_TARGET` 通道（之前只在系统性故障时用），
不做去重，同一条问题会天天发一遍。详见 CHANGELOG.md。

## 已在 `1.0.1` 修复

**P0：企微在线表格解析函数 `_cells()` 会把「第一列或最后一列恰好为空」的行
整行错位一格，企业名、分行、日期全部串到别的字段里，不报错、不留痕。**

`wecom_doc._cells()` 原来用 `line.strip("|")` 去掉一行首尾的 `|`。
Markdown 表格里某一列恰好为空时，那一列自己的 `|` 会跟行首/行尾的边界 `|`
连在一起（比如「序号」列为空，行首就是连续两个 `|`）。`.strip("|")` 会把
**连续多个** `|` 一并当成边界吃掉，那一空列凭空消失，后面每一列全部
错位一格——「企业」这一列读到的其实是下一列的值，真正的企业名被
挤进了再后一列。三张企微台账（舆情主台账、AI体检报告主台账、
GEO主台账）共用这一个函数，GEO 主台账 439 行里有 293 行「序号」为空，
命中比例最高；影响范围随各台账「序号」列的实际填写习惯而定。

改法：只删首尾各一个真正的边界 `|`（`s[1:]` / `s[:-1]`），不用
`.strip("|")` 做连续删除。

🔴 **副作用，业务需要知道**：GEO 主台账里「序号」和「企业」两列组成这张
台账的身份识别主键，缺一不可。之前「序号」为空的行会被错误地拼出一个
非空的假身份、蒙混过关；修复后系统能正确识别出「序号」缺失，会**整张
台账拒绝处理当天的催办判定**（而不是漏掉这一行、其余照常）——这是既有
护栏的设计（宁可全表停摆也不做出错误判定），不是本次改动引入的新行为。
业务往 GEO 主台账新增项目时，「序号」必须同步填，否则当天这张台账会
整体停摆，需要业务自己在文档里补上后才能恢复。

**P0：腾讯文档解析函数 `read_sheet()` 会把加粗过的表头格读成空字符串，
影响 AI节能盒子、AI安全员/舆情/体检/GEO 各自的前期台账共 5 张台账。**

排查上一条 bug 时顺带发现——这 5 张腾讯文档台账突然集体报「表头缺少
必需列」。核实后确认数据完全没丢：表头下面每一行的真实数据都在，
只有第 0 行（表头）那一格读出来是空，业务确认那一格文字确实在、
没有合并单元格，只是**加粗**了。复现：同一个单元格，腾讯文档接口的
「cells 结构化模式」（原来固定用的模式）读出来 `value_type`/
`string_value` 都是空，换成「CSV 导出模式」读完全正常——是腾讯文档
接口本身对这一类表头格的结构化响应有缺陷。

改法：表头单独换成 CSV 模式取，数据行继续用 cells 模式不变（P0-3
早已说明数据不能整体走 CSV——日期在 CSV 模式下依赖单元格显示格式）。
🔴 取 CSV 文本时不能简单按物理行切：AI外贸拓客台账的表头格里业务自己
写了带换行的使用说明，CSV 规范里这种字段会被引号包起来，物理行数比
逻辑行数多，按物理行切会在字段中间断开——必须用 `csv.reader` 解析
整段文本取第一条逻辑记录。

952 项测试通过（企微 2 项 + 腾讯文档 5 项，均先在旧代码 / 改错的中间
版本上确认会失败）。真实生产数据核对：此前失败的全部 5 张台账加
AI外贸拓客恢复正常读取。

## 已在 `0.6.0-rc1` 修复 / 新增

**六类「配置写错了但不报错」的写法，离线校验现在一律拦下。**
它们此前能过 JSON 语法、能过 `doctor --validate-config`、运行时也不抛错，
只是让那个节点从此不再产生催办 —— 一条日志都没有，业务只是再也收不到那条提醒。
与 `0.4.0-rc9` 修的 `{"days": -3}` 完全同一类。

`{"op":"in"}` 漏 `values`（恒假，节点一条都不催）｜
`{"op":"contains"}` 漏 `value`（恒真，节点吞掉所有项目、后面的节点全部轮空）｜
条件漏 `field`（取值恒为空串）｜启用节点 `when: []`（恒假）｜
`enabled: "false"` 字符串（是真值，想停用却把节点静默开着）｜
`op` 写错（原本要等到次日 9:00 运行时才抛错）。

🔴 `scope_filters` 走的是同一个 `match_condition`，同样的写法在那边后果更大：
**整张台账被过滤光**，表现只是「今天没有要催的」。两处共用同一份
`core.condition_errors`，不各写一遍。

**新增需求文档变更探测。** 每天推送前自动核一次《跟进精灵需求文档》有没有被改过。
它**只回答变没变，不回答变了什么** —— 替代的是「每天打开文档扫一眼」，
不替代「读懂改了什么」。价值在于文档没变的那些天可以完全不看。

> ⚠️ **这项能力有个明确边界：它只认腾讯文档。** 取指纹走的是
> `qqdoc.file_fingerprint`（`manage.query_file_info`）。需求文档若哪天搬到
> 企业微信文档或飞书，这一段就得另做适配 —— 企微的 `sheet_get_info`
> **不返回修改时间**（见上面那条），飞书同样没有等价指纹。
> 换句话说：**换了平台不会报错，只会变成再也不提示**，
> 与它本要消灭的失败模式同形。搬文档时必须一并处理。

**新增 `doctor.py --values <台账id>` 与 `scripts/diff_due.py`。**
前者只读列出判据用到的各列真实取值（改触发条件前必看）；
后者把「改动前后待催名单逐条比对」从肉眼活变成一条命令。
操作卡见 `docs/改一个催办节点.md`。

## 已在 `0.4.0-rc12` 修复

**飞书从零接入不再在「重新登录」和「添加协作者」之间来回跳。**

- 新增 `docs/飞书多维表格接入.md`，按应用 profile、API scope、同一 profile 的
  user 登录、文档协作者四道门顺序验收。
- 所有示例显式携带 `--profile`；无 profile 的 `auth login` 不再作为安装指令。
- 明确 `/share/base/view/shr...` 不是 `base_token`，必须取得原始 Base 链接或真实 ID。
- lark-cli 的 `type/subtype/code/missing_scopes/hint` 不再被丢弃。自检分别识别
  profile、登录、scope、资源权限和 macOS 钥匙串问题，只给当前层的一条建议。
- 当前代码固定 `--as user`，作者应添加实际登录账号为协作者，不应添加机器人。

## 已在 `0.4.0-rc10` 修复 / 新增

**接入三份企微文档主台账，补齐舆情 / AI体检 / GEO 的 5 个催办节点。**
卡了五天的能力授权 2026-08-10 通过，当天完成阶段 1 实测 + 全部接入。

| 变化 | 说明 |
|---|---|
| **新增数据源 `wecom_doc`** | 只读白名单只有两个命令。`wecom-cli doc` 底下实测 25 个子命令，其中 8 个能改能删，**永远不进白名单** |
| **`key_field` 支持组合键** | 三份企微台账**一个唯一列都没有**（编号/序号各有重复，GEO 还有 4 行空序号），而重复主键会让两个项目共用一条催办状态、互相静音。实测「编号/序号 + 企业」全部唯一 |
| **计时起点支持人工播种** | GEO 那张表一个可靠日期列都没有（`6.12` 没年份、`结束优化时间` 混着「优化中」「/」）。播种表放配置仓、每条写明依据；**排在真实快照之后**，快照永远优先 |
| **主键断言改为范围感知** | 责任范围外的空主键/重复主键降级为数据质量警告。GEO 有 4 行把「提问关键词」误填进企业列（范围内 0 行），原来会让整份 132 行的台账读不了 |

🔴 **本轮唯一真正的验收点是故障可见性**，已实测：把 url 改坏后
退出码 `1`、报「企微文档接口报错：errcode=640002 …这不是「今天没有要催的」」。
实测拿不到权限时 JSON-RPC 外层是 `"isError": false`，错误只藏在
`result.content[0].text` 解出来的 `errcode` 里 —— 只看外层就会读成「0 行」。

⚠️ **GEO 这条线的判据建立在一列自由文本上**（`启动优化时间`，全表 43 种写法），
是本项目唯一这么做的地方。护栏是把 43 种全列进 `known_values`：
出现没见过的写法会当天报数据质量警告（只进日志），否则那一行会**静默落出所有节点**
——既不催也不终止。**建议让业务在这张表上加一个固定取值的状态列和一个带年份的日期列。**

## 已在 `0.4.0-rc9` 修复

**rc8 把节律写法放开给业务自己改，那么「改错了」从此是常态而不是意外。
这一版补的就是「改错了会怎样」—— 全部由 Codex 复审提出。**

| 问题 | 原来会怎样 | 现在怎样 |
|---|---|---|
| **`repeat` 写成不是对象**（如 `"repeat": "每周一"`，照着中文说明写最容易这样） | 离线校验放行，定时任务真读台账时一句 `AttributeError` —— 校验函数自己成了错误本身 | 离线就报「repeat 必须是对象」 |
| **`{"days": -3}`** | 放行，而且**不报错**：判定是 `(今天−上次).days >= -3`，负数恒真，节律**静默变成每天提醒**，配置看起来却是配过的。这一组里最坏的一种 | `days`/`workdays` 必须是 ≥1 的整数，否则离线报错 |
| **`{"days": "七"}` / `{"days": 7.9}`** | 前者运行时 `int()` 炸，后者被静默截断成 7 | 同上，离线拦下 |
| **`{"weekday": [], "days": 7}`** | 排他检查看的是「值真不真」，空数组是假的，于是当成「只配了 days」放行。这正是业务改到一半的形状（旧值删空了、键忘了删，又加了一行新的）——**节律看着改了、实际没改** | 改成看「键在不在」，同时在场就报「只能配一种」 |
| **`doctor` 自带一份 `repeat` 解读** | ① 数组直接打印成 `只在每周['Mon', 'Thu']提醒`，业务读不懂；② 它的必填检查漏了 rc8 新增的 `monthday`，配 `monthday` 的节点会被**误报「缺 repeat」** | 删掉那份复制品，与主流程共用 `core` 的同一份；口径表显示「周一/周四提醒」 |

🔴 这一版的教训与 rc6 `alert.timeout_seconds`、rc8 `repeat` 离线校验是**同一个形状**：
**同一件事写了两份实现，迟早只有一份是对的。**
`doctor` 的口径表是给业务核对「规则改成什么了」的凭证，
它说的话必须和推送里说的是同一句 —— 所以只能有一份实现。

## 已在 `0.4.0-rc8` 修复

**起因是业务反馈「某个项目至今没发货，怎么不提醒」。重读她的需求文档
（腾讯文档「跟进精灵🧚需求文档」）逐条比对，发现文档改过而配置没跟上。**

| 问题 | 原来会怎样 | 现在怎样 |
|---|---|---|
| 哨兵④发货的**节律与需求文档不一致** | 文档已改成「每周一和每周四提醒」，配置仍是「每周三」。于是周一周四业务看不到它，而她正是在周一来问的 | 配置改为 `["Mon","Thu"]`，与文档一致 |
| `repeat.weekday` **只收一个星期几** | 「每周一和每周四」这种纯口径改动**必须改代码才能表达** —— 业务每改一次节律就要等一次发版 | 收字符串或数组；另新增 `monthday`（每月固定几号），四种写法齐了 |
| 同时配两种节律**被静默忽略一种** | 判定是 `if weekday … elif workdays … else days`，配了 `weekday` 又配 `days` 的话 `days` 整个不生效，而配置看上去两种都写了。**业务改节律最可能的动作正是「加一行新的、忘了删旧的」** | 配置校验期直接报错，不许这么写 |
| **静默期项目在任何地方都看不见** | 已催过、在等下一个提醒日的项目从清单上完全消失，与「判定认为不用催」长得一模一样。业务因此以为系统漏了项目，排查也无从下手 | 日志里列出「【静默期】N 项」，含上次提醒日与节律。**不进企微推送**（业务明确只要日志） |
| 盒子①收资 | 需求文档里已无此节点（全文搜「收资」0 次），配置仍启用 | 停用并留痕；`⏸ ①收资：未启用` 会出现在报告里，不静默 |

🔴 **`repeat` 支持的四种写法**（业务改提醒规则时查这里，四种**只能配一种**）：

| 写法 | 含义 |
|---|---|
| `{"days": 7}` | 每 7 天提醒；`1` 就是每天 |
| `{"workdays": 2}` | 每 2 个工作日提醒 |
| `{"weekday": ["Mon","Thu"]}` | 每周一、周四提醒（单个写成 `"Wed"` 也行） |
| `{"monthday": [1,15]}` | 每月 1 号、15 号提醒；配 `31` 时当月不足则按月末算 |

生效的节律会显示在推送的阶段行上（如「【待发货】3 项 · 周一/周四提醒」），
**改了当天就能看出来生效的是什么**，不用等「怎么一直不提醒」才发现。

## 已在 `0.4.0-rc7` 修复

| 问题 | 原来会怎样 | 现在怎样 |
|---|---|---|
| 范围过滤告警**发送失败也被记成「已告警」** | rc6 只要判定出「整表落空」就把台账写进去重登记，不看告警到底有没有发出去。**告警失败那次撞上的正是最需要它的时候**（网络抖动、hermes 不在 PATH），而登记一旦写下，后续每天都被去重跳过 —— 这条业务线从此永久静默，只能等人主动翻日志 | 只有告警**发送成功**才登记；失败不写登记，下一次真实运行继续重试 |

🔴 **这是 rc6 自己制造的坑，而且是同一个形状的第二次**：0.3.0-rc1 修过一模一样的
「发不出去却记成已通知」（见 `tests/test_alert.py` 那条老护栏），rc6 新写去重逻辑时
又长了一遍。**凡是「发送 + 去重登记」成对出现的地方，登记都必须以发送成功为前提。**
`tests/test_scope_emptied_alert.py::test_failed_alert_is_retried_and_not_deduplicated` 守着这条。

## 已在 `0.4.0-rc6` 修复

**Codex 代码审查提的两条 P1，都验证成立后修的。**

| 问题 | 原来会怎样 | 现在怎样 |
|---|---|---|
| `alert.timeout_seconds` 填错让告警**裸崩** | 填「三十」这类非数字 → 告警函数在重试之前抛 `ValueError`。而它**只在已经出事时**被调用，等于把「有故障」升级成「主脚本崩掉、连故障是什么都说不出来」；离线校验当时还报「通过」 | 离线校验拦下非正整数；运行时再兜一道，配置写错就退回默认 30 秒继续告警 |
| 台账被范围过滤光时**只有本地日志知道** | rc4/rc5 会在报告里写一行，但那一刻待催数是 0：企微无事不发、告警通道碰不到、看门狗只看到「任务成功」。**没人主动翻日志，这条业务线已经全量失效而无人知晓** | 变空那天告警一次，一直空着不重复，恢复时记一笔并清掉登记。退出码仍是 0 |

🔴 **后一条的反面同样重要**：这条警告**不能**进 `run_warnings`。
`exit_code == 0 and not run_warnings` 才写 `last_full_success`，
而看门狗靠它判断「任务有没有跑」。塞进去会让每天都少一次成功记录，
两天后看门狗误报「任务根本没跑」——**修好一个静默，换来一个假警报**。
节假日闸门当初踩的就是这个坑，`tests/test_scope_emptied_alert.py` 专门守着。

## 已在 `0.4.0-rc5` 修复

| 问题 | 原来会怎样 | 现在怎样 |
|---|---|---|
| **故障告警被一次网络抖动整条打掉** | `hermes send` 一次不重试。2026-08-06 09:00 Telegram 有约 50 秒的 `httpx.ConnectError` 窗口，告警正好落在里面 → **业务既没收到催办，也没收到「出事了」** | 三次尝试，间隔 2s / 5s，每次的原因都记进 `health.json` |

🔴 **依据是实测，不是拍脑袋。** 对照：告警成功的 08-03/04/05 三天，同一时间窗内
Telegram 网络错误数都是 **0**；失败的 08-06 是 **4** 条。真正说明问题的是同一天
09:07 的日志——每日新闻的推送撞上同样的 `ConnectError`，写着
`retrying in 1s (attempt 1/3)`，**重试后送达了**。同一天、同一个网络，
有重试的成功、没重试的失败。

告警是「业务完全收不到催办」时最后一道让人知情的机制，
它自己被几十秒的网络抖动打掉，正是这个项目一路在消灭的那类静默失败。

## 已在 `0.4.0-rc4` 修复

**这一轮的主角是「上一轮宣告修复的那个 P0 又原样发作了」。**

| 问题 | 原来会怎样 | 现在怎样 |
|---|---|---|
| **飞书连续两天整天读不到**（rc2 那条的复发） | rc2 只剔了 `HERMES_HOME` / `OPENCLAW_HOME`。实测 `lark-cli` 的 Agent 上下文探测是**一张固定清单**，另有四个同样触发。真凶 `HERMES_EXEC_ASK` 由 Hermes gateway 在**进程启动后**注入，定时任务原样继承 → 每天静默无数据 | 清单补齐为六个，并留下「实测枚举、只增不减」的注释与测试。**刻意不用 `HERMES_*` 前缀通配** —— 实测编造的同前缀变量不触发，通配等于凭空猜上游语义 |
| 告警失败只留一句退出码 | `hermes send` 退出码非零而 stderr 为空时，`health.json` 里只有「退出码 1」，错误信息其实在 stdout 里、被整个丢掉 | stderr 与 stdout 都记入原因；两个都空时明说「无输出」 |
| 责任范围过滤把整表清空却不出声 | 台账里换个写法（「深圳分行」改成「深圳」）就会全表落空，而输出只是「总任务量：0 个项目里」——**和「今天没有要催的」一模一样** | 过滤后无剩余行时记数据质量警告并列出实际取值。不影响判定与退出码（一条线暂时没有范围内项目是合法状态） |

🔴 **这一轮真正该记住的教训，不是补了几个变量名，而是当时的验证方法本身是错的。**
rc2 宣告修复的依据是「查过 gateway 进程的环境，只有 `HERMES_HOME`」。
但 `ps eww` 只显示 **exec 时的初始环境**，而这些变量是进程起来之后在 Python 里
`os.environ[...] = ...` 塞进去的（`gateway/run.py` 的 `HERMES_EXEC_ASK`、
`cli.py` 的 `HERMES_QUIET`）。**看进程环境快照 ≠ 看子进程真正拿到的环境。**

这和 rc2 自己记下的那条教训是同一个形状：`env -i` 复现 cron 时，把环境洗得
比生产更干净。两次都是「验证环境与生产不一致」，只是方向相反。
唯一可靠的做法是**在真实调用路径上捕获真正交给 `subprocess.run` 的那份 env**
—— 现在测试里就是这么做的。

## 已在 `0.3.0-rc4` 修复

**这一轮收拾的是上一轮修复自己带进来的问题。** 教训值得记下来：
`0.3.0-rc2` 用「遇到空锁一律让路」堵住了一个竞态，却因此造出一个永久死锁——
**同一个函数、同一类症状（每天静默跳过）**。靠"让路"回避竞态，等于把
"有人正在写"和"有人写到一半死了"这两种状态永久混为一谈。

| 问题 | 原来会怎样 | 现在怎样 |
|---|---|---|
| 空锁文件永久堵死运行 | 进程在建锁与写内容之间被强杀 → 盘上留下 0 字节锁 → **此后没有任何进程能拿到锁**，每天静默跳过 | 锁的创建改为「先写完整临时文件 → `os.link` 发布」，**0 字节窗口从根上不存在**；另加 `flock` 守卫串行化检查/抢占/释放 |
| 并发时抢锁、释放锁、临时文件互相干扰 | 多个任务同时启动会覆盖对方的临时文件，把正常运行判成失败 | 探针与状态各用独立临时文件 |
| 升级沿链接覆盖项目目录外的文件 | 清理旧代码时跟随符号/硬链接写到 Skill 之外 | 拒绝跟随链接 |
| 升级中断丢源文件 | 移动半途失败可能源已删、目标没写成 | 改为可回退的两段式 |
| 中文/空格/特殊字符路径生成无效 plist | `render_plist` 是字符串拼接、无 XML 转义 → launchd **静默**不加载 | 加 XML 转义；已在中文与空格路径下完成隔离安装实测 |
| 23 点被算成 24 点 | 深夜时刻边界溢出 | 已修 |
| 看门狗无法安全更新自身 | 自更新失败或原地覆盖留下半个脚本，监控器从此不可执行 | 改为原子替换 |
| 损坏状态/异常计数/未来时间让看门狗崩溃或长期静默 | 状态被写坏或时钟回拨后要么崩、要么再也不告警 | 逐项容错并退回默认 |
| 自动化测试会触发真实系统通知 | 独立副本的用例误触真实告警通道 | 已隔离 |

## 已在 `0.3.0-rc2` 修复

**这一轮全部集中在监控链路自身**——监控器比被监控对象更脆弱，等于没有监控。

| 问题 | 原来会怎样 | 现在怎样 |
|---|---|---|
| 抢锁标记残留 | 进程在抢陈旧锁的半路被强杀（关机、OOM），标记留在盘上 → 下次算出同一个标记名 → **每天静默跳过，永远** | 入口先扫残留、超 120 秒的标记判为残留并重试一次；四进程 × 15 轮真并发守着「自愈不破坏互斥」 |
| 告警失败进了去重 | 告警发失败**照样**记进 24 小时去重 → 下次「已告警过，不重复」→ **告警永远发不出去** | 去重键只在发成功后才写；失败只留诊断字段，下次必定重试 |
| 本机通知失败被当成成功 | `osascript` 退出码根本没看 → 没有 GUI 会话/权限被拒时仍报「已告警」并退 0 | 检查退出码，非零降到第三级（写日志 + 非零退出） |
| 配置写错直接崩 | `schedule_hour: "九"` → ValueError；`25` → 更隐蔽，到 `time(25,0)` 才炸。**监控器崩掉 = 完全没有监控** | 逐字段校验类型与范围，坏值退默认 + 人话警告，绝不罢工 |
| 监控器住在 Skill 里 | Skill 被移动/删除/升级装坏 → plist 指向的文件没了 → launchd 静默失败。**而「Skill 坏了」正是它最该报警的场景** | 装到 `<运行时目录>/watchdog/`，与 Skill 解耦；实测删掉整个 Skill 后仍能判定并告警 |
| 升级留下旧版代码 | 上一版的 `.py` 还躺在 `scripts/` 里，仍可 import、仍会被 `discover` 跑到 | 清单内的残留移进 `.upgrade-backup-<时间戳>/`（**移动不是删除**）；业务配置、状态、凭证逐字节不动 |
| 定时口径三处不一 | README 说周一至周五，`install.sh` 和生产实际是每天 | 全仓统一，并有测试扫描全部交付文件（口径本身在 0.4.0-rc1 又改回每天，见上文第 5 条） |
| plist 硬编码 Python | `/usr/bin/python3` 在精简系统和 Linux 上并不存在 | 安装时检测并写入实际路径 |
| **定时任务下飞书整天读不到**（0.4.0-rc2 修，~~已解决~~ **只修了一半，见下方 rc4/rc5**） | 完整环境传给 `lark-cli`，`HERMES_HOME` 被当成 Agent 上下文信号，未绑定时拒绝执行 | 外部 CLI 子进程环境剔除 `HERMES_HOME` / `OPENCLAW_HOME`，并有源码守卫测试 |
| **两个进程能同时拿到运行锁**（不在报告清单里，新写的并发测试逼出来的） | 锁的创建不是原子的：`os.open(O_EXCL)` 与 `os.write` 之间文件是 0 字节，而 0 字节和「锁文件损坏」无法区分 → 被当成陈旧锁夺走 → 创建者与夺锁者**同时在跑** | 空锁一律判为「创建者正在写」，让路不夺；有内容的损坏锁仍照夺 |

🔴 **业务能感知的变化：周末不再推送。** 停滞天数按自然日算，周末不推不会让数字失真；
而周末推等于教业务周末忽略它，长期反而伤害降噪。

## 已在 `0.3.0-rc1` 修复

| 原第几条 | 问题 | 现在怎样 |
|---|---|---|
| 4 | 缺少独立看门狗 | ✅ 已交付 `watchdog.py` + launchd 模板 + 安装文档。**但需你实际安装才闭环**，见上面第 4 条 |
| 5 | 零催办时本地没有任何回执 | ✅ 真实运行必留一行摘要；有清单时在末尾追加投递结果。企微仍然无事不发 |
| 6 | `health.json` 自身损坏没有独立告警 | ✅ 它现在第一个被读，与其余状态文件共用「登记 → 隔离保留 → 告警 → 记恢复事件」，且那一次不算完整成功 |
| 7 | 陈旧锁存在并发竞争 | ✅ 锁加了不可复用的 owner token，抢占改为互斥后 `os.replace`，释放前核对归属。四进程 × 15 轮真竞态测试守着 |

## 已在 `0.2.0` 系列修复

- 合法 JSON 但字段类型错误时，给出可理解的配置错误，不再裸崩。
- `--dry-run`、`--json`、`--today` 诊断模式增加只读闸门，失败路径也不发消息、不写状态。
- 推送失败不再记为「已通知」；退出码区分正常/主任务失败/启动故障。
- 显示的天数改为「超期天数」，跨阶段可横向比较。

## 本轮不包含

- 从微信聊天记录自动识别项目进展并回填台账。
- 自动修改腾讯文档台账。
- ~~企业微信文档台账适配层（原因见上文）。~~ **0.4.0-rc10 已实现**（只读，两个命令的白名单）。
- 任何写入类企微命令 —— `sheet_update_range_data` / `sheet_append_data` /
  `smartsheet_delete_*` / `edit_doc_content` 等**永远不进白名单**。
- 正式的 Windows 安装与运行支持。
