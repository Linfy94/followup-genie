# 已知问题

本文件对应 `0.2.0-rc2`。这些问题不阻止小范围安装测试，但不应被描述为“已正式上线”。

## 业务测试前必须知道

1. **WorkBuddy 尚未做端到端实机验证。** 官方文档确认能上传本地技能包，但没有公开
   完整的包格式细节；本次同时提供 `.skill` 和 `.zip` 供实测。
2. **WorkBuddy 定时运行会发起 Agent 任务。** 与 Hermes 的 `--no-agent` 不同，
   会消耗相应积分，也受模型、权限确认和客户端在线状态影响。
3. **Windows 尚未验证。** 当前安装脚本使用 Bash，macOS 提醒事项依赖 AppleScript。
   Windows 只能作为后续适配目标，不能列入本轮验收。
4. **缺少独立看门狗。** 如果电脑休眠、WorkBuddy/Hermes 没启动或定时任务被删除，
   主任务本身无法主动告警；需要另一个独立任务检查 `health.json`。
5. 🔴 **零催办时本地没有任何回执（已确认的缺陷，不是待测项）。** 当天没有要催的项目时，
   脚本的标准输出是空的，Hermes 会把这次运行记为 `[SILENT]`。**业务手动点一下，看到的
   就是「毫无反应」。** 企微不发是对的（无事不发），但本地运行结果不该跟着一起消失。
   下一版修复：真实运行至少输出一行摘要（形如「检查完成：读取 101 项，待催 0 项，
   静默期 23 项，企微未发送」）。碰到时请当作已知问题，不是你操作错了。
6. **状态文件损坏恢复仍需人工关注。** 当前会保留损坏文件并降级运行，但
   `health.json` 自身损坏时的独立告警链路还需加强。
7. **陈旧锁存在极低概率并发竞争。** 多个实例同时抢占陈旧锁时，仍需要更强的原子保护。

## 已在当前源码修复

- 合法 JSON 但字段类型错误时，改为给出可理解的配置错误，不再裸崩。
- `--dry-run`、`--json`、`--today` 诊断模式增加只读闸门，失败路径也不发消息、不写状态。

## 本轮不包含

- 从微信聊天记录自动识别项目进展并回填台账。
- 自动修改腾讯文档台账。
- 对腾讯文档以外的数据源做通用适配。
- 正式的 Windows 安装与运行支持。
